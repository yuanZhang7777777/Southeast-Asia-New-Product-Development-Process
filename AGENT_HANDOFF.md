# Agent Handoff

> Updated: 2026-08-11 13:42 Asia/Shanghai
> Purpose: current facts, hard boundaries, and the next executable sequence only. Detailed history stays in `docs/20-项目推进总控.md` and `C:\Users\86173\.codex\work-logs\`.

## Read Order

1. This file: current scope and stop conditions.
2. `docs/2026-07-09-已确认需求记录.md`: confirmed business rules, highest priority.
3. `docs/20-项目推进总控.md`: detailed delivery timeline and recorded read-backs.
4. `docs/21-后半段需求领导对齐问题清单.md`: downstream workflow, PLM/FineBI facts, and unresolved business inputs.
5. `docs/02-功能实现状态.md`: implementation and test status.
6. `docs/06-部署与服务器准备.md`: environment, backup, deployment, and rollback procedures.
7. `AGENTS.md`: project safety and engineering rules.

Do not recover decisions from old release notes or screenshot-only discussions when they conflict with the files above or the user's newest message.

## Immediate Operating Boundaries

- The worktree is deliberately dirty and contains work from multiple agents. Start every implementation task with `git status --short`, inspect the relevant diff, and never run `git reset --hard`, `git checkout --`, or overwrite unrelated changes.
- The active code baseline is `main` at or after merge commit `59429cf`. For deployment, data migration, or rollback, cite the exact commit being used.
- This root `AGENT_HANDOFF.md` is the only active handoff. Any `AGENT_HANDOFF-*` file found in older branches or worktrees is historical evidence only and must not override this file.
- Production changes require a fresh explicit user approval, a production backup, a dry-run/read-back report, and the deployment runbook. Do not infer approval from an old handoff line.
- `hz-new-product-dev` is the only active development environment after the 2026-07-30 Hong Kong cutover. The former development Compose stack is down with zero containers and no `18081` listener; its seven named volumes and verified database backup remain for recovery only and must not be restarted.
- The active development frontend is `/assets/index-BqQmk99W.js`, Alembic is `a7b8c9d0e123 (head)`, and development `DINGTALK_CARD_AUTOSEND_ENABLED=false` / `PLM_SYNC_ENABLED=false`. `WORKFLOW_AUTOMATION_ENABLED` remains the pre-existing `true`; the selection2 and selection3/4 history migrations created no downstream task or notification. Do not change these switches without explicit approval.
- Development account passwords remain isolated from production. The Hong Kong cutover copied account/role configuration but not production password hashes, so a production password is not expected to authenticate against development. On 2026-07-31 `刘学城` was confirmed enabled with `super_admin`, and a development-credential login smoke test returned HTTP 200. Do not copy or reset password hashes across environments without explicit approval.
- During documentation-only work, do not touch either database, deployment, OSS, PLM, FineBI, or DingTalk.
- Never commit credentials, cookies, tokens, `.env`, or personal passwords.

## Current Objective

Production is live at `hz-new-product-preprod:/opt/hengzhe-new-product/app` with `PLATFORM_BASE_URL=http://101.132.26.138:8080`. DingTalk autosend is enabled, `PLM_SYNC_ENABLED` remains false, and daily PLM arrival notification is handled by the guarded 10:00 cron `/opt/hengzhe-new-product/app/run_plm_daily_notify.sh` instead of the long-lived scheduler. Current PLM new-arrival rule is authoritative: `first_listing_time` Beijing date must equal the target date; do not use `latest_storage_time` equality for new-arrival detection.

2026-08-11 10:18 Asia/Shanghai production HCD022/period cleanup: HCD022 root cause read-back showed three production opportunities/claims existed for 陈丽妹 (`HCD022PK/HCD022WT/HCD022YL`, source `selection1_20260714.xlsx`, batch `开发0714期`), but no `plm_arrival_item`/`arrival_record` contained HCD022 and the claims had blank `downstream_status`, so the secondary-research workbench could not show them. Under `/opt/hengzhe-new-product/.codex_locks/new_product_workflow_prod.lock`, backup `/opt/hengzhe-new-product/app/backups/hcd022_secondary_fix_20260811_101627/db_before.sql.gz`, the three claims were opened to `waiting_secondary_research` via existing service logic and three manual `arrival_record` rows were created with audit action `arrival.manual_hcd022_correction`. Read-back: all three HCD022 opportunities and claims are `waiting_secondary_research`, `secondary_research_submitted_at` is blank, each has one arrival record, and `services.list_secondary_research_groups(db, "陈丽妹", "__all__", "waiting_secondary_research")` returns one HCD022 group with the three sub SKUs. Then unused period `历史归档` was removed under the same lock after dry-run confirmed 12 opportunities, 12 source snapshots, 12 listing bindings, and 0 claims/reviews/stocking/arrivals; backup `/opt/hengzhe-new-product/app/backups/remove_unused_period_20260811_101756/db_before.sql.gz`. Read-back: `历史归档` opportunities/import batches are 0 and product-board periods no longer include it. Production `/api/health` OK. Local checks: `python -m pytest backend/tests/test_secondary_research.py::test_historical_claim_already_waiting_secondary_research_can_be_filled backend/tests/test_product_board.py::test_product_board_business_periods_use_active_board_scope -q` returned 2 passed; `node --test tests/secondaryResearchDrafts.test.ts tests/productBoard.test.ts` returned 21 passed. Note: production `plm_arrival_item.raw_payload` had zero HCD022 text hits, so future investigation should focus on whether the source PLM workbook actually contains HCD022 and whether `first_listing_time` date filtering excluded it before ingestion.

2026-08-11 10:38 Asia/Shanghai production HCD022 follow-up code deploy: arrival notification grouping now uses `ArrivalRecord` as the source of truth and accepts current secondary sources `platform/history_selection1/plm_arrival_discovery/manual_secondary`; it no longer sends raw PLM rows without an opened secondary-research task, and it also covers manual arrival corrections without a `plm_arrival_item`. Business-day matching for manual arrival records uses the Beijing date converted to a UTC range. Production backup before file replacement: `/opt/hengzhe-new-product/app/backups/notify_arrival_record_deploy_20260811_103559/`; `api`, `worker`, and `scheduler` were rebuilt/recreated because production containers bake code into the image. Read-back: `/api/health` OK, containers healthy, hidden period dropdown intersection is `[]`, `历史归档` opportunity count is 0, 陈丽妹待二调 still contains one HCD022 group with 3 SKUs, and `_arrival_groups(db, "2026-08-11")` returns 陈丽妹 `HCD022(3)` from the manual arrival correction. Local regression: `python -m pytest backend/tests/test_notification_jobs.py -q` returned 22 passed; focused combined backend regression returned 25 passed.

2026-08-11 12:07 Asia/Shanghai production PLM archive-blocker fix: the 2026-08-10 PLM run had 10 true new-arrival rows but only 7 were opened because three rows were blocked by old hidden system records (`history_selection34` / `historical_archive`). Production `backend/app/plm_processing.py` now treats only current visible opportunities as existing-system blockers; read-only historical archives no longer suppress `PLM新增到货` discovery. A narrow backfill helper `activate_existing_system_sku_discoveries(...)` was added and applied only to `arrival_date=2026-08-10`, converting three previously `existing_system_sku` rows to `matched_discovered`: 江琴 `ZR-SH-BI-268/ZR-SH-BI-268`, 江琴 `XUHG1622/XUHG1622BN8`, and 陈丽妹 `CJA4623/CJA4623A1`. DB backup before apply: `/opt/hengzhe-new-product/app/backups/plm_existing_system_reconcile_20260811120157.dump`; code backup: `/opt/hengzhe-new-product/app/backups/codex/plm_processing.py.20260811120026.bak`. Read-back: 2026-08-10 arrival records are 10/10, `PLM新增到货` waiting-secondary rows are 18, and arrival notification logs for 2026-08-10 are 6 receiver groups; 江琴 and 陈丽妹 were newly sent after the backfill while existing dedupe logs for the other receivers were reused. Production `/api/health` OK and `api/scheduler/worker` are up. Local regression: `python -m pytest backend/tests/test_product_board.py backend/tests/test_plm_processing.py backend/tests/test_historical_arrival_activation.py backend/tests/test_secondary_research.py::test_read_only_historical_claim_cannot_open_arrival_or_secondary_research backend/tests/test_secondary_research.py::test_historical_claim_already_waiting_secondary_research_can_be_filled -q` returned 40 passed. Note: `开发0505期` is not hidden; production currently has no rows for that batch. The old dirty sources were not physically deleted in this pass beyond the previously recorded `历史归档` cleanup.

2026-08-11 13:32 Asia/Shanghai local PLM assignment-gate implementation: unmatched PLM new-arrival rows must not be auto-assigned to the PLM salesperson. Exact matches still require current visible platform ownership (`site/country + main SKU + child SKU + current claim owner`) and continue through the existing ArrivalRecord -> waiting secondary-research -> DingTalk path. Rows that do not exact-match a current visible owner, including missing platform products or old hidden/archive-only blockers, now stay on the PLM item as `pending_assignment` with PLM salesperson preserved only as audit evidence. Managers/super-admins get a new secondary-research tab `到货待分配`; they can assign one pending PLM row to an enabled operator, which then creates/reuses the platform opportunity/claim, writes ArrivalRecord, and opens that selected operator's secondary-research task, or close the row with a reason. Before manager assignment, no operator task and no arrival notification are produced. This local change is not deployed yet. Verification: `python -m pytest backend/tests/test_plm_processing.py backend/tests/test_secondary_research.py backend/tests/test_notification_jobs.py -q` returned 62 passed; `npm --prefix frontend run build` passed; `git diff --check` passed on touched files.

2026-08-11 13:42 Asia/Shanghai local PLM assignment-gate follow-up: the `到货待分配` API now returns explicit assignment guidance. If a PLM row matches multiple current visible products by `country/site + main SKU + child SKU`, response field `assignment_block_reason` is `当前系统存在多个同国家+主SKU+子SKU商品，需先处理商品归属`; the frontend shows this Chinese reason and disables `指派进二调` for that row. Single-match rows say assignment will enter the selected operator's secondary research and notification candidate; no-match rows say assignment will create a `PLM新增到货` product first. Verification: focused assignment tests returned 3 passed, and full related regression `python -m pytest backend/tests/test_plm_processing.py backend/tests/test_secondary_research.py backend/tests/test_notification_jobs.py -q` returned 63 passed; `npm --prefix frontend run build` passed. Still not deployed to production.

2026-08-11 16:57 Asia/Shanghai production PLM 2026-08-03 through 2026-08-10 pending-assignment cleanup: user confirmed that on 2026-08-11 the current PLM intake should cover through 2026-08-10, not 2026-08-11. Production read-back showed `plm-2026-08-10.xlsx` has 10 new-arrival rows and all are already reasonably in the system (`matched`/`matched_discovered`: 8 waiting secondary, 2 already submitted/listed), so no 2026-08-10 rows were moved to pending assignment. For 2026-08-03 through 2026-08-09, 41 old `existing_system_sku` blockers remained visible to the PLM arrival-assignment gate: dry-run classified 37 real business rows to queue, 3 duplicate rows from `到货通知-8.6.xlsx` to close because equivalent `plm-2026-08-05.xlsx` rows exist, and 1 `UAT0804-...` test row to close. Under `/opt/hengzhe-new-product/.codex_locks/new_product_workflow_prod.lock`, backup `/opt/hengzhe-new-product/app/backups/prod_before_plm_pending_assignment_20260811_.dump`, the 37 rows were updated to `match_status='pending_assignment'` with `_plm_assignment.reason='PLM新品到货被旧隐藏历史记录误阻断；等待主管/超管指派当前负责人'`; the 4 non-business/duplicate rows were updated to `assignment_closed`; 41 audit rows were inserted. No opportunities, claims, arrival records, secondary-research records, listing records, or DingTalk notifications were created. Read-back: 2026-08-03 through 2026-08-10 has `pending_assignment=37`, `assignment_closed=4`, and 2026-08-10 remains only `matched=3` / `matched_discovered=7`. `services.list_plm_arrival_assignments` returns 59 total pending-assignment rows: 37 from 2026-08-05/08-07/08-08 plus 22 older 2026-07-28 through 2026-08-01 rows that were not changed in this pass. Note: that service read took about 45 seconds because it checks current product matches per row; optimize before larger-scale use.

2026-08-09 23:01 Asia/Shanghai production DingTalk arrival-card mobile hotfix: user reported PLM arrival cards were hard to read on mobile because SKU details were packed into the one-line `summary_text`, and that tapping the card body could open the website. Production `backend/app/dingtalk_card_sender.py` now keeps arrival `summary_text` count-only (`<date> 到货待二调 N 个主 SKU`), renders SKUs one per line in `sku_markdown` as `1. MAIN（n子SKU）`, and no longer appends the plain system URL to arrival-card body text. Todo cards still retain the plain system entry fallback. Local checks: `python -m pytest backend/tests/test_dingtalk_card_sender.py -k "arrival_card"` returned 5 passed; `python -m pytest backend/tests/test_dingtalk_auto_notifications.py -k "dingtalk_action_url"` returned 1 passed. Production deploy target `hz-new-product-preprod:/opt/hengzhe-new-product/app`, lock `/home/lxc/HengzheProjects/.codex_locks/new_product_workflow_prod.lock`, backup `/opt/hengzhe-new-product/app/backups/hotfix_arrival_card_mobile_20260809_225356/`; `api/scheduler/worker` rebuilt and production `/api/health` OK. Verification card `arrival-mobile-format-test-20260809` sent only to 刘学城 with summary `2026-08-07 到货待二调 3 个主 SKU` and line-based SKU markdown. If tapping blank card body still opens the website, fix the DingTalk card template by removing the root/container click action and keeping only the button bound to `action_url`; backend cannot separate those if the template binds the same variable at the container level.

2026-08-09 23:06 Asia/Shanghai follow-up correction: the first mobile hotfix proved the DingTalk arrival template does not visibly render `sku_markdown`; user screenshot still showed only `summary_text` and `left_count`. Production was patched again so arrival `summary_text` itself contains the visible line-based SKU list (`<date> 到货待二调 N 个主 SKU\n1. MAIN（n子SKU）...`, limited to the first 8 with a “查看全部” suffix). Backup `/opt/hengzhe-new-product/app/backups/hotfix_arrival_card_visible_sku_20260809_230457/`; `api/scheduler/worker` rebuilt and production `/api/health` OK. Container read-back printed summary with three lines of SKU, and test card `arrival-visible-summary-test-20260809` was sent only to 刘学城 with DingTalk `success=True`.

2026-08-09 23:20 Asia/Shanghai new secondary/arrival card-template compatibility: user provided template `9aef77f1-daa1-4362-8b6d-08c77378ae13.schema`, whose visible variables are `card_title/summary_text/left_label/left_count/right_label/right_count/tip_text/action_text/action_url/actionyrl/length`. Production `backend/app/dingtalk_card_sender.py` now populates those variables for arrival-to-secondary-research cards: SKU lines stay in visible `summary_text`, left count is main SKU count, right count is total child SKU count, `action_url` and typo-compatible `actionyrl` both point to the same secondary-research URL, and `length` matches the visible line count. The formal production `DINGTALK_ARRIVAL_CARD_TEMPLATE_ID` was not changed yet; one-off preview used the new template id. First one-off send failed because production user table still stores 刘学城 as placeholder `account:刘学城`; second send used the known real test userId `03630624351420944192` and DingTalk returned `deliverResults.success=True`. Production deploy backup `/opt/hengzhe-new-product/app/backups/hotfix_secondary_template_params_20260809_231448/`; local checks `python -m pytest backend/tests/test_dingtalk_card_sender.py -k "arrival_card"` returned 5 passed and `python -m pytest backend/tests/test_dingtalk_auto_notifications.py -k "dingtalk_action_url"` returned 1 passed; production `/api/health` OK. Wait for user visual confirmation before changing `.env` `DINGTALK_ARRIVAL_CARD_TEMPLATE_ID` to this new template id.

2026-08-09 23:22 Asia/Shanghai rich-text arrival/secondary card preview: user created template `db53706e-cf17-4214-a70d-39d83a052db0.schema` so SKU details live in a real rich-text variable `md`, allowing selection/copy without using the root clickable text. Production `backend/app/dingtalk_card_sender.py` now also outputs `md` with the same line-based SKU detail as `sku_markdown`; formal production template id remains unchanged. One-off preview card `arrival-md-template-preview-20260809-2321` used this template, with `md="**新品**\n1. GZMO086（5子SKU）..."`, and was sent to known real 刘学城 userId `03630624351420944192`; DingTalk returned `deliverResults.success=True`. Backup `/opt/hengzhe-new-product/app/backups/hotfix_secondary_template_md_20260809_232056/`; local checks `python -m pytest backend/tests/test_dingtalk_card_sender.py -k "arrival_card"` returned 5 passed and `python -m pytest backend/tests/test_dingtalk_auto_notifications.py -k "dingtalk_action_url"` returned 1 passed; production `/api/health` OK. User asked “刘学成”, but production `刘学成` is disabled/no DingTalk userId; test used the historically confirmed 刘学城 userId.

2026-08-09 23:28 Asia/Shanghai rich-text template click-isolation test: user reported clicking/copying the SKU rich text still opens the website, so the remaining problem is likely a DingTalk template-level root/container/rich-text click action, not the backend SKU text. No code/env change. Three one-off cards were sent to 刘学城 real userId with template `db53706e-cf17-4214-a70d-39d83a052db0.schema`: `arrival-md-click-isolation-A-20260809-2328` (`action_url` blank, `actionyrl` URL), `...-B-...` (`action_url` URL, `actionyrl` blank), and `...-C-...` (both blank). All DingTalk deliveries returned `success=True`. Wait for user to report which card has non-clickable SKU text and working button; if none, the template must remove click action from the root/card/rich-text component and leave action only on the button.

2026-08-10 00:02 Asia/Shanghai a331 rich-text template preview: user removed `actionyrl` and created template `a331c983-30d1-46d4-b6dc-267e03538a57.schema`, showing only button component bound to `action_url`. No code/env change and formal `DINGTALK_ARRIVAL_CARD_TEMPLATE_ID` remains `71d31a67-195e-4192-b10f-44984b796d9a.schema`. One-off test card `arrival-a331-template-preview-20260810-0001` was sent to 刘学城 real userId `03630624351420944192` with only variables `card_title/summary_text/md/left_label/left_count/right_label/right_count/tip_text/action_text/action_url`; DingTalk returned `deliverResults.success=True`. Production `/api/health` OK. Wait for user to confirm whether rich-text SKU copying no longer triggers navigation and whether only the button navigates.

2026-08-10 00:10 Asia/Shanghai 3f30 template click-isolation test: user provided template `3f30ff56-8a72-4392-a888-974b3c9a2883.schema` after `a331` still jumped when clicking the card body. No code/env change and formal `DINGTALK_ARRIVAL_CARD_TEMPLATE_ID` remains `71d31a67-195e-4192-b10f-44984b796d9a.schema`. Two one-off cards were sent to 刘学城 real userId `03630624351420944192`: `arrival-3f30-click-test-A-button_url-20260810-0010` (`action_url` blank, `button_url` production secondary-research URL) and `arrival-3f30-click-test-B-action_url-20260810-0010` (`action_url` URL, `button_url` blank). Both DingTalk deliveries returned `success=True`. Wait for user to report whether A achieves “SKU rich text can be selected/copied without jumping, button jumps”; if yes, backend should add `button_url` and blank `action_url` only for the new formal arrival template before switching `.env`.

2026-08-10 00:18 Asia/Shanghai formal 3f30 arrival card switch: user confirmed A (`button_url` version) allows copying SKU text without accidental navigation and the button still opens secondary research. Production at `hz-new-product-preprod:/opt/hengzhe-new-product/app` now uses `DINGTALK_ARRIVAL_CARD_TEMPLATE_ID=3f30ff56-8a72-4392-a888-974b3c9a2883.schema`. `backend/app/dingtalk_card_sender.py` now treats this template as click-safe by writing the real secondary-research link to `button_url` and blanking `action_url`/`actionyrl`; other arrival templates keep legacy `action_url` behavior. Production backup `/opt/hengzhe-new-product/app/backups/3f30_template_20260810_001545/`; lock `/opt/hengzhe-new-product/.codex_locks/new_product_workflow_prod.lock`; services `api/worker/scheduler` rebuilt. Read-back: API health OK, container config template is `3f30...`, payload has `action_url=''`, `button_url='http://101.132.26.138:8080/?from=ding&role=operator&view=research'`, `actionyrl=''`. Verification card `arrival-3f30-formal-prod-test-20260810-0018` was sent only to 刘学城 and DingTalk returned `deliverResults.success=True`. Note: before confirming the correct host, the same backend/template change was also applied to the older `hermes-remote` dev stack `/opt/hengzhe-new-product-dev/app` with backup `/opt/hengzhe-new-product-dev/app/backups/3f30_template_retry_20260810_001325/`; no formal operator cards were sent from that stack.

2026-08-10 00:32 Asia/Shanghai read-only FineBI/Item schedule check: production `hz-new-product-preprod:/opt/hengzhe-new-product/app` currently does not automatically pull FineBI Item data into observation periods. Production crontab only has PLM daily notify at 10:00; `.env` has `FINEBI_AUTO_PULL_ENABLED=true` but no active scheduler flag; production `backend/app/scheduler.py` has no FineBI pull branch; scheduler log only shows `scheduler started for production; plm_sync=False`; `scheduler_job_run` has PLM/daily-notification rows but no `finebi_pull` rows. The codebase has a manual super-admin endpoint `POST /api/admin/finebi/pull` that calls `finebi_auto_pull.pull_and_import(...)`, and local/newer scheduler code intends a weekly pull after 08:00 Beijing for the latest completed Thu-Wed week, retrying every 300s on failure, but this is not live in production yet. Current platform metric apply boundary is `services.apply_week_metrics(...)`: it only updates an existing active `shop+item+platform period`, changes `pending_data` to `pending_review`, and never creates fake zero rows when there is no FineBI source row.

2026-08-07 10:26 Asia/Shanghai production PLM/secondary correction: the PLM parser, processing hash, and arrival idempotency are deployed. Rows are included as new arrivals only when `首次上架时间` date equals the target date; the same workbook can be processed for multiple target dates without hash collision; duplicate arrival facts for the same claim are skipped. A cleanup reopened 1,278 mislocked secondary-research responsibilities that had no listing evidence, all secondary fields blank, and were incorrectly marked `historical_secondary_submitted`; 371 blank-field rows with listing evidence and 209 rows with secondary fields were kept. Final dry-run read-back: `reopened=0`, `kept_with_listing=371`, `kept_with_fields=209`. Read-back: `waiting_secondary_research=1292`; `WPHG511BG` and `WPHG511OG` for 李干 are both editable pending secondary research and each has one arrival record. Production backups: `/opt/hengzhe-new-product/app/backups/20260807_100951_plm_secondary_fix/`, `/opt/hengzhe-new-product/app/backups/20260807_101254_plm_hash_fix/`, `/opt/hengzhe-new-product/app/backups/20260807_101543_plm_idempotent_fix/`, and `/opt/hengzhe-new-product/app/backups/20260807_101735_plm_0727_0806_apply/`. The workbook `E:/soft/dingding/到货通知-8.6.xlsx` was copied to production temp and processed for 2026-07-27 through 2026-08-06: 108 first-listing new rows and 91 matched/planned responsibilities. Real arrival cards for 2026-07-27 through 2026-08-06 were sent by date/operator group; recent read-back shows 89 `sent` and 6 historical `skipped_no_receiver` rows in that range. `卢壁兰` was later mapped and retried successfully; 2026-08-06 skipped receiver names still missing production user/role mappings are `侯悦悦`、`孟浩雨`、`张定发`、`曾程晓`、`邱纯玲`. Product image URLs read back as 4,792/4,792 OSS URLs and 0 local paths. Local regression `python -m pytest backend/tests/test_plm_arrivals.py backend/tests/test_plm_processing.py backend/tests/test_historical_secondary_status_cleanup.py backend/tests/test_secondary_research.py -q` returned `39 passed`; production `/api/health` OK.

2026-08-07 10:50 Asia/Shanghai secondary-research workbench scope correction: user clarified the current pending secondary-research workbench must contain only the 2026-07-27 through 2026-08-06 arrival-notice Excel items, while push-workbook rows are treated as already secondary-researched/listed facts and market-monitor rows are not arrival evidence. Production cleanup ran under `/opt/hengzhe-new-product/.codex_locks/new_product_workflow_prod.lock`: first removed 1,000 waiting-secondary rows with no arrival fact, no listing binding, and blank secondary fields; then removed 248 older PLM-batch waiting-secondary rows not sourced from `到货通知-8.6.xlsx`. No product rows, arrival records, listing records, or submitted secondary facts were deleted. Backups: `/opt/hengzhe-new-product/app/backups/20260807_1040_secondary_scope_fix/` and `/opt/hengzhe-new-product/app/backups/20260807_1048_secondary_keep_notice_excel/`. Final read-back: `waiting_secondary_research` is 44 rows / 24 groups, `non_notice_excel_waiting_rows=0`, and all remaining waiting rows have arrival source `到货通知-8.6.xlsx`; production `/api/health` OK.

2026-08-07 11:09 Asia/Shanghai HCK021 secondary draft copy: user confirmed the draft saved on duplicate HCK021BU responsibility should be copied to the current 8.6 arrival secondary-research group for confirmation. Production backup: `/opt/hengzhe-new-product/app/backups/hck021_secondary_draft_copy_20260807_110852/db.dump`, SHA256 `814e7923a43a2a3484179ee4a8656893147273c955bfe92011e144dce3fd0fa9`. Copied non-empty draft fields from duplicate claim `abbce1a2-6ae2-41ab-a79e-27bff87a28ef` to current `HCK021BK/HCK021BU/HCK021PK/HCK021WT`: competitor URL, conclusion `无变化`, product positioning `利润款`, and one OSS image. Target daily sales and selling points were not copied because the source draft is blank for those fields. No submitted timestamp was written; all four rows remain `waiting_secondary_research` for 陈丽妹 to confirm and complete. Audit action `secondary_research.draft_copied_for_confirmation` was written 4 times. A scan for the same duplicate-draft pattern across current `到货通知-8.6.xlsx` waiting rows found 0 remaining candidate groups.

2026-08-04 development follow-up: the secondary-research and listing/observation workbenches now use a five-minute frontend page-data cache. Normal tab switches render cached data immediately and refresh in the background; explicit refresh or write actions force a fresh API read. Development DB was also cleaned so submitted secondary-research facts no longer remain in `waiting_secondary_research`: final read-back is `historical_secondary_submitted=1858`, `waiting_secondary_submitted=0`, and `waiting_secondary_unsubmitted=9`. The cleanup backups are `/opt/hengzhe-new-product-dev/app/backups/dev_secondary_cache_cleanup_20260804_104658/` and `/opt/hengzhe-new-product-dev/app/backups/dev_secondary_status_cleanup_remaining_20260804_104910/`. A one-off safe notification test sent three development cards with autosend overridden only for that run: arrival 1, elimination summary 1, manager review 1, listing 0. These cache/status/data changes remain development-only; production later received only the DingTalk card URL hotfix recorded below.

2026-08-04 DingTalk card URL hotfix: all automatically generated DingTalk card action URLs now use only the environment base URL, with query/fragment removed and a trailing `/` forced. Do not append `from=ding`, `role`, `view`, `module`, task ids, or identity parameters. Development backend images for `api`, `worker`, and `scheduler` were rebuilt and recreated; dev read-back for `http://139.224.2.166:18081/` returns exactly `http://139.224.2.166:18081/`. Production was patched afterward as recorded below. When production sends real cards, production `PLATFORM_BASE_URL` must be `http://101.132.26.138:8080`, and card buttons must carry `http://101.132.26.138:8080/`, not the development URL.

2026-08-04 full-chain UAT read-back is complete in development. Report: `/opt/hengzhe-new-product-dev/app/backups/uat_full_flow_20260804_111932/uat_full_flow_report.json`; result `32 passed / 0 failed`. Fresh verification before production hotfix: `python -m pytest backend/tests/test_plm_processing.py backend/tests/test_notification_jobs.py backend/tests/test_product_board.py -q` returned `41 passed`; `npm run build` returned Vite build success with `/assets/index-BqQmk99W.js`. Production card URL hotfix was then applied through SSH alias `hz-new-product-preprod` at `/opt/hengzhe-new-product/app`, not `hermes-remote`. Earlier backup directory: `/opt/hengzhe-new-product/app/backups/dingtalk_action_url_prod_hotfix_20260804_121046/`; latest backup directory for the trailing-slash fix: `/opt/hengzhe-new-product/app/backups/code_dingtalk_url_20260804_182806/`. Only `backend/app/services.py` was patched; `api`, `worker`, and `scheduler` images/containers were rebuilt/recreated. Production read-back: `PLATFORM_BASE_URL=http://101.132.26.138:8080`, `DINGTALK_CARD_AUTOSEND_ENABLED=false`, `PLM_SYNC_ENABLED=false`, API health `healthy`, unauthenticated protected API returns `401`, and container `dingtalk_action_url(...)` returns exactly `http://101.132.26.138:8080/` for operator and supervisor. Five production test cards were then sent only to 刘学城: `生产测试：到货通知`、`生产测试：运营新品待办`、`生产测试：已到货新品催办`、`生产测试：主管新品待办`、`生产测试：淘汰款提醒`; all logged as `sent` with dedupe prefix `prod-card-link-uat-` and printed button URL `http://101.132.26.138:8080/`.

2026-08-04 18:40 Asia/Shanghai DingTalk button follow-up: the five test cards above displayed but their buttons did not open in the DingTalk client. Root cause is the createAndDeliver payload carrying a naked HTTP/IP+port action URL. Local fix: `backend/app/dingtalk_card_sender.py` now wraps outgoing card payload `action_url` with DingTalk's unified open-link protocol `dingtalk://dingtalkclient/action/open_platform_link?...`, while business services still pass only the environment base URL. Local verification `python -m pytest backend/tests/test_dingtalk_card_sender.py backend/tests/test_notification_jobs.py backend/tests/test_dingtalk_auto_notifications.py -q` returned `46 passed`. Production patch was applied at `hz-new-product-preprod:/opt/hengzhe-new-product/app`; backup `/opt/hengzhe-new-product/app/backups/code_dingtalk_open_link_20260804_183559/`; `api`, `worker`, and `scheduler` images/containers were rebuilt/recreated. Production read-back confirmed payload action URL starts with `dingtalk://dingtalkclient/action/open_platform_link` and embeds `http://101.132.26.138:8080/`. One verification card `生产链接修复验证` was sent only to 刘学城 with dedupe `prod-card-openlink-fix-20260804103648`; user confirmed it opens. After confirmation, six corrected cards were sent only to 刘学城 and read back as `sent`: `生产联调测试：PLM/FineBI（修正版）`, `生产测试：到货通知（修正版）`, `生产测试：运营新品待办（修正版）`, `生产测试：已到货新品催办（修正版）`, `生产测试：主管新品待办（修正版）`, `生产测试：淘汰款提醒（修正版）`; dedupe timestamp `20260804104315`.

2026-08-04 18:49 Asia/Shanghai final DingTalk card entry rule: user confirmed the direct visible URL in the `生产系统入口` card works. Do not rely on buttons or per-card deep links as the only entry. `backend/app/dingtalk_card_sender.py` now also appends the plain system entry to every outgoing card body: todo cards append `系统入口：<base_url>` to `tip_text`; arrival-template cards append it to `sku_markdown`. Local verification `python -m pytest backend/tests/test_dingtalk_card_sender.py backend/tests/test_notification_jobs.py backend/tests/test_dingtalk_auto_notifications.py -q` returned `48 passed`. Production patch backup: `/opt/hengzhe-new-product/app/backups/code_dingtalk_plain_entry_20260804_184736/`; production read-back shows both todo and arrival payload bodies include `系统入口：http://101.132.26.138:8080/`. Six `系统网址版` cards were sent only to 刘学城 and read back as `sent` with dedupe timestamp `20260804104853`.

2026-08-04 18:58 Asia/Shanghai final DingTalk external-browser rule: user rejected DingTalk in-app opening. `backend/app/dingtalk_card_sender.py` now wraps outgoing button links with `dingtalk://dingtalkclient/action/jump?url=<encoded base url>`, so the card button requests an external browser jump; visible card body still includes `系统入口：http://101.132.26.138:8080/` as fallback. Production patch backup: `/opt/hengzhe-new-product/app/backups/code_dingtalk_external_browser_20260804_185451/`; `api`, `worker`, and `scheduler` were rebuilt/recreated. Production payload read-back returned `dingtalk://dingtalkclient/action/jump?url=http%3A%2F%2F101.132.26.138%3A8080%2F`; API health is OK. Final test card `生产测试：外部浏览器跳转（最终版）` was sent only to 刘学城 and read back as `sent`, dedupe `prod-external-browser-card-test-20260804105848`. If a DingTalk client still intercepts the button, use the visible URL line; do not reintroduce per-card deep links or query parameters.

2026-08-04 19:03 Asia/Shanghai DingTalk PC link variant test: user pointed out `action/jump` is mobile-only/PC-limited. No code change was made for the variant test. Five one-off production cards were sent only to 刘学城 with distinct button `action_url` values to identify the PC behavior: `生产链接测试1：01普通HTTP` (`http://101.132.26.138:8080/`), `生产链接测试2：02open_platform_link`, `生产链接测试3：03page_link`, `生产链接测试4：04page_link_pc_slide`, and `生产链接测试5：05page_link_jump`. Dedupe prefix `prod-link-variant-20260804110308-`; all five notification logs read back `sent`; production health OK. Wait for the user to report which numbered card opens the external browser before changing the default sender again.

2026-08-04 19:08 Asia/Shanghai final DingTalk PC/prod card rule: user confirmed `生产链接测试1：01普通HTTP` works on the PC client. `backend/app/dingtalk_card_sender.py` was changed back to keep button `action_url` as the plain environment base URL; no `dingtalk://` wrapping is used by default. Visible body entry remains `系统入口：http://101.132.26.138:8080/`. Local verification `python -m pytest backend/tests/test_dingtalk_card_sender.py backend/tests/test_notification_jobs.py backend/tests/test_dingtalk_auto_notifications.py -q` returned `48 passed`. Production patch backup: `/opt/hengzhe-new-product/app/backups/code_dingtalk_plain_http_20260804_190726/`; `api`, `worker`, and `scheduler` were rebuilt/recreated; production payload read-back returned `action_url=http://101.132.26.138:8080/`; public `/api/health` OK. Final card `生产测试：普通HTTP最终版` was sent only to 刘学城 and read back `sent`, dedupe `prod-plain-http-final-card-20260804110819`.

2026-08-05 09:21 Asia/Shanghai production launch-day notification preview: user asked to enable `PLM_SYNC_ENABLED` and send Liu Xuecheng the cards each real user would receive today before real rollout. Production `.env` was changed to `PLM_SYNC_ENABLED=true` under `.codex_locks/new_product_workflow_prod.lock` and scheduler restarted; backup `/opt/hengzhe-new-product/app/backups/env_enable_plm_sync_20260805_091654/`. Scheduler immediately entered restart loop with status `Restarting (137)` while reading PLM workbook, so `PLM_SYNC_ENABLED` was reverted to `false` to protect production; backup `/opt/hengzhe-new-product/app/backups/env_disable_plm_sync_after_oom_20260805_091832/`. Do not re-enable automatic PLM sync until the PLM job is made safe outside the long-lived scheduler or memory usage is fixed. Preview cards sent only to 刘学城 with preview dedupe and no real-user dedupe consumption: `preview-real-arrival-2026-08-05-20260805092016-01-UAT运营A3830`, `preview-real-arrival-2026-08-05-20260805092016-02-UAT运营C3830`, and `preview-real-manager-review-2026-08-05-20260805092016`; all read back `sent`. Read-back blockers before real send: 2026-08-04 arrival groups are UAT test operators (`UAT运营A3830` restock 1, `UAT运营C3830` new_arrival 1), operator and manager notification mappings are disabled (`operator enabled true notification false: 20`, `manager enabled true notification false: 1`), only `super_admin` notification is enabled. Current safe state: `PLM_SYNC_ENABLED=false`, `DINGTALK_CARD_AUTOSEND_ENABLED=false`, scheduler up, API health OK.

2026-08-05 09:36 Asia/Shanghai production launch-day notification correction: user confirmed all operator/manager notification mappings should be enabled. Production role mappings were backed up to `/opt/hengzhe-new-product/app/backups/enable_role_notifications_20260805_092550/before_enable_role_notifications.dump` and updated from `operator=20 false + manager=1 false` to `operator=20 true + manager=1 true`; global autosend remains `DINGTALK_CARD_AUTOSEND_ENABLED=false`. The earlier supervisor preview `722/0` was a code bug: the notification function counted historical/imported `NewProductOpportunity.current_status` rows, while the supervisor review page counts only current platform claim rows. `backend/app/services.py` now counts only `SalesClaimForecast.source_column='platform'` rows without a `ReviewRecord`; local notification/scheduler regression returned `62 passed`. Production patch backup: `/opt/hengzhe-new-product/app/backups/code_supervisor_review_count_20260805_093303/`; `api`, `worker`, and `scheduler` images/containers were rebuilt/recreated. Read-back: public `/api/health` OK, `count_pending_claim_reviews=0`, `count_pending_not_claim_reviews=0`, operator/manager notification mappings enabled, `PLM_SYNC_ENABLED=false`, `DINGTALK_CARD_AUTOSEND_ENABLED=false`, scheduler logs `plm_sync=False`. Today’s supervisor review card should not send. The current PLM scheduler code is conceptually daily (`08:00 Asia/Shanghai`, previous Beijing date, calls PLM export and `process_plm_arrival_workbook`), but production cannot guarantee it while scheduler is capped at 192 MiB and exits 137 on PLM parsing. Next step before enabling real PLM auto-sync: move PLM into a guarded one-off job or fix the scheduler memory/resource path, then verify one complete daily run under lock.

2026-08-05 09:52 Asia/Shanghai PLM notification launch correction in progress: user authorized real production use. A one-off module `backend/app/plm_daily_notify_job.py` was added and deployed so PLM runs in the `api` container under `.codex_locks/new_product_workflow_prod.lock`, not in the 192 MiB scheduler loop. Production backup before PLM notification run: `/opt/hengzhe-new-product/app/backups/plm_notify_20260805_0943/prod_before_plm_notify.dump` SHA-256 `f1ffbeb38929fa89df3a241d6fe9ae2b8d3c79c1626db024be44c9348eaf7f57`. The 2026-07-27 through 2026-08-04 one-off processed all nine dates and created/read scheduler job runs for those dates. However the first 29 range cards were redirected to Liu Xuecheng because `.env` still had `DINGTALK_TEST_RECIPIENT_USER_ID=03630624351420944192`; those cards showed `测试模式｜原收件人` and should be treated as preview/test sends, not successful real-operator delivery, despite `NotificationLog.send_status='sent'`. The test recipient user id was then cleared and `api/worker/scheduler` recreated; backup `/opt/hengzhe-new-product/app/backups/env_disable_dingtalk_test_recipient_20260805_094932/`. Read-back now: `DINGTALK_CARD_TEST_RECEIVER_NAME=''`, `DINGTALK_TEST_RECIPIENT_USER_ID=''`, `DingTalkCardConfig.test_recipient_user_id=''`. A single verification arrival card `生产到货按钮验证` was sent only to Liu with payload `action_url=http://101.132.26.138:8080/` and no test-mode marker. Wait for user confirmation before resending the 2026-07-27..2026-08-04 real arrival notifications to operators with fresh dedupe keys and before adding the daily 10:00 cron; an attempted cron write failed from quoting and did not install a crontab entry.

2026-08-05 09:58 Asia/Shanghai production PLM arrival notifications are live. User bound the new DingTalk template variables and requested arrival template id `71d31a67-195e-4192-b10f-44984b796d9a.schema`; production `.env` was backed up to `/opt/hengzhe-new-product/app/backups/env_arrival_template_20260805_20260805_095217/` and now sets `DINGTALK_ARRIVAL_CARD_TEMPLATE_ID=71d31a67-195e-4192-b10f-44984b796d9a.schema`. Verification card `生产到货新模板验证` used that template, plain HTTP `action_url=http://101.132.26.138:8080/`, no test-mode marker, and the user confirmed it works. The 2026-07-27 through 2026-08-04 range was then resent to real operators with fresh dedupe suffix `real-20260805-0955`: 29 groups total, 28 `sent`, 1 `skipped_no_receiver` for `卢壁兰` because no DingTalk receiver mapping/userId was available. Real sent names: 冯卓宏、庞莹莹、方晓冰、李干、李桂敏、江琴、潘露英、赵钰婷、连香香、郑纯华、陆伟豪、陆俊全、陈丽妹、饶培丽. Daily production cron is installed and crond is active: `0 10 * * * /opt/hengzhe-new-product/app/run_plm_daily_notify.sh >> /opt/hengzhe-new-product/logs/new_product_plm_daily_$(date +\%Y\%m\%d).log 2>&1`; cron backup `/opt/hengzhe-new-product/app/backups/cron_plm_daily_notify_20260805_095644/`. Current read-back: public `/api/health` OK, `DINGTALK_CARD_TEST_RECEIVER_NAME=''`, `DINGTALK_TEST_RECIPIENT_USER_ID=''`, arrival template id matches, `run_plm_daily_notify.sh` is executable. Follow-up: add/fix DingTalk mapping for 卢壁兰 if she should receive future PLM arrival cards.

2026-08-05 10:01 Asia/Shanghai follow-up: user created/fixed the `卢壁兰` operator mapping with DingTalk userId `485055084721238705`. Production read-back showed `enabled=True` and `notification_enabled=True`. The skipped 2026-08-03 arrival group was resent with dedupe `dingtalk_card:arrival:2026-08-03:卢壁兰:real-20260805-userid-retry` and read back `sent`. Keep the earlier skipped log `...:real-20260805-0955` as audit evidence; do not delete it.

2026-08-05 10:06 Asia/Shanghai clarification: production PLM files were downloaded/processed for `plm-2026-07-27.xlsx` through `plm-2026-08-04.xlsx` (recent batch read-back: 7/27 1082 rows, 7/28 1008, 7/29 1313, 7/30 1480, 7/31 1431, 8/01 1227, 8/02 1513, 8/03 2623, 8/04 2434). The product-board period labels `PLM到货新增SKU20260727-0802` and `PLM到货新增SKU20260803` are not the full PLM download windows; they only represent PLM arrivals that were absent from the platform and therefore were补建 as new pending secondary-research SKU batches. Read-back showed 8/04 PLM had 12 `new_arrival` rows, but they matched historical/existing records (`historical_secondary_research` / `matched_historical`) and did not create a separate `PLM到货新增SKU20260804` discovery batch.

2026-08-05 10:16 Asia/Shanghai production rule/deploy update: per user confirmation, all future PLM-discovered system-missing SKU placeholders must use one fixed visible business period `PLM新增到货`; do not create date-suffixed PLM discovery periods. Local TDD check updated `test_plm_processing_creates_discovery_secondary_research_for_missing_system_sku` and passed. Production code updated under lock in `/opt/hengzhe-new-product/app`: `backend/app/plm_processing.py`, `backend/app/plm_local_ingest.py`, `frontend/src/secondaryResearchDrafts.ts`, `frontend/src/SecondaryResearchView.tsx`; frontend now has no `最新期数` option in secondary research period filter. Production DB merged existing `PLM到货新增SKU20260727-0802` and `PLM到货新增SKU20260803` opportunity batches into `PLM新增到货` (8 records). Backup: `/opt/hengzhe-new-product/app/backups/plm_fixed_period_20260805_1012/`; an earlier partial backup also exists at `/opt/hengzhe-new-product/app/backups/plm_fixed_period_20260805_1010/`. Read-back: `/api/health` OK, all containers running, backend code contains `period = "PLM新增到货"`, DB PLM periods are `[("PLM新增到货", 8)]`, frontend container no longer contains `最新期数` / `latestSecondaryResearchPeriod`.

2026-08-05 10:25 Asia/Shanghai production DingTalk autosend is enabled. User asked to open `DINGTALK_CARD_AUTOSEND_ENABLED`; production `.env` was backed up to `/opt/hengzhe-new-product/app/backups/env_enable_dingtalk_autosend_20260805_1025/`, then `api/worker/scheduler` were recreated. Read-back from API settings: `autosend=True`, `test_name_set=False`, `test_userid_set=False`; `/api/health` OK. Verification also confirmed PLM files `plm-2026-07-27.xlsx` through `plm-2026-08-04.xlsx` were processed with row counts 1082/1008/1313/1480/1431/1227/1513/2623/2434 and new-arrival counts 16/12/2/9/38/6/36/1/12. Real arrival notification logs for dedupe `real-20260805*`: 30 total rows, 29 `sent`, 1 old `skipped_no_receiver` for 卢壁兰 at `real-20260805-0955`, plus retry `dingtalk_card:arrival:2026-08-03:卢壁兰:real-20260805-userid-retry` read back `sent`. Cron remains installed at 10:00 daily via `/opt/hengzhe-new-product/app/run_plm_daily_notify.sh`.

2026-08-05 11:33 Asia/Shanghai secondary-research arrival status hotfix is live in production. Root cause: PLM arrival and manual secondary-research entry opened `SalesClaimForecast.downstream_status='waiting_secondary_research'` but left/created `NewProductOpportunity.current_status='claim_submitted'`, so these records appeared in supervisor review even though they were already secondary-research tasks. Local fix: `services.open_secondary_research()` now also sets opportunity status to `waiting_secondary_research`, manual secondary-created opportunities start at `waiting_secondary_research`, and historical arrival activation no longer writes `claim_submitted`. Regression `python -m pytest backend/tests/test_secondary_research.py backend/tests/test_historical_arrival_activation.py backend/tests/test_review_flow.py -q` returned `47 passed`. Production backup: `/opt/hengzhe-new-product/app/backups/secondary_status_fix_20260805_113111/`; `api/worker/scheduler` were rebuilt/recreated. Data cleanup updated exactly 2 existing wrong rows: `ZXHG3326/ZXHG3326-SV` and `HCK021/HCK021BU`. Read-back: `bad_review_secondary_count=0`, public `/api/health` OK. Same-main-SKU children such as `HCK021PK/BK/WT` may still show `claim_submitted` when they are normal claim-review records and do not have secondary-research downstream status.

2026-08-05 11:40 Asia/Shanghai dashboard pending-review count hotfix is live in production. User saw the home/status card still showing `待复核 699` after the secondary status cleanup. Root cause: supervisor review notification/page counts had been fixed, but the home card still used frontend fallback `claim_submitted + claim_rejected` from product-board status, which includes historical/imported claim states. Backend `/dashboard/counts` now returns `pending_claim_reviews` and `pending_not_claim_reviews` from the same real platform-review count functions used by supervisor notifications; frontend home metrics use those backend counts instead of local status fallback. Local verification: `python -m pytest backend/tests/test_dashboard_counts.py backend/tests/test_review_flow.py -q` returned `22 passed`; `node --test frontend/tests/homeMetrics.test.ts` returned `7 passed`; `npm --prefix frontend run build` passed. Production backup: `/opt/hengzhe-new-product/app/backups/dashboard_review_count_fix_20260805_113909/`; `api/worker/scheduler/frontend` were rebuilt/recreated. Read-back: `pending_claim_reviews=0`, `pending_not_claim_reviews=0`, served asset `assets/index-CJP0z31a.js` contains `pending_claim_reviews`, public `/api/health` OK. During this deploy an operator upload to `/api/opportunities/import/selection1/upload` with `Content-Length=146548773` collided with API recreation and returned 502 after about 30s; no new `ImportBatch` was created, so the user should retry that import after refreshing the page.

2026-08-05 12:00 Asia/Shanghai selection1 7.29 import/image repair is live in production. Root cause of broken assignment-table images and messy product information: production `selection1_importer.py` still used old selection1 columns (`F=image_url`, `G=main_sku_name`, `H=main_sku`, `J=sub_sku`) and two-row header parsing, while the current 7.29 template is `E=二级类目`, `F=关键词`, `G=产品图片`, `H=主SKU名称`, `I=主SKU`, `J=子SKU名称`, `K=子SKU`, `L=产品类型`, `M=开品理由`. This wrote keywords such as `Túi du lịch có bánh xe` into `image_url` and produced combined header labels such as `物流方式/海运` in product details. Local fix updated `selection1_importer.MAIN_COLUMNS`, product-image fallback column, and `services.OPPORTUNITY_FIELD_COLUMNS`; regression `python -m pytest backend/tests/test_selection1_import.py backend/tests/test_selection1_caigen_extended.py backend/tests/test_opportunity_edit.py -q` returned `23 passed`. Production backup and DB dump: `/opt/hengzhe-new-product/app/backups/selection1_729_import_fix_20260805_115309/`; `api/worker/scheduler` were rebuilt/recreated. The already-uploaded workbook `/app/.private_uploads/source-workbooks/b9a70536940b425f9259550b4a59590b-新品0804期.xlsx` sheet `开发0804期` was re-imported under the same business period, updating 256 rows, creating 1 row that the old parser had skipped, skipping 1 invalid source row, and extracting OSS product images. The old invalid row `HXS012 / 260短款背带【加固口袋 】绿色` from source row 86 had no real `子SKU` in column K and was precisely disabled (`601a7b96-d7af-44cc-8799-96c8fa67a46f`); normal `HXS012BK` and `HXS012CE` remain active. Read-back: 257 visible 0804 rows, 257 valid image URLs, 0 bad visible image values; HYK036/HYK037/HYK038 image URLs are OSS product images; HYK038 snapshot headers are clean `A:站点 ... M:开品理由`; public `/api/health` OK.

2026-08-05 12:12 Asia/Shanghai production data correction: user confirmed `开发0804期` rows `HYK034` through `HYK038` are one product whose main SKU must be `HYK034`. After backup `/opt/hengzhe-new-product/app/backups/hyk034_main_sku_fix_20260805_121223/`, four opportunity rows were corrected from `HYK035/HYK036/HYK037/HYK038` to `HYK034` under `/opt/hengzhe-new-product/.codex_locks/new_product_workflow_prod.lock`. Source snapshots were kept unchanged for Excel audit. Read-back: `HYK034_group_count=5`, old main SKU remaining count `0`, all five rows retain HTTP OSS image URLs, production health OK.

2026-08-05 13:32 Asia/Shanghai production emergency fix: API 502 was caused by OOM/restart during full product-board/home requests and deploy rebuilds, not data loss. A 4G `/swapfile-new-product` swap file is active and persisted in `/etc/fstab`; stale OOM one-off containers were removed. Frontend no longer loads hidden archives on first page load. Product board now defaults to 300 backend groups and prefilters group keys in SQL before loading rows; production read-back `services.list_product_board_groups(limit=300)` is about 0.7s and `query='HYK034'` about 0.02s. Backend assignment confirmation now skips stale selected IDs that are no longer `pending_assignment` and only assigns still-pending rows; if none remain it asks the user to refresh. Production backups: `/opt/hengzhe-new-product/app/backups/frontend_reduce_initial_load_dist_20260805_130656/`, `/opt/hengzhe-new-product/app/backups/product_board_service_prefilter_20260805_132022/`, `/opt/hengzhe-new-product/app/backups/assignment_stale_skip_20260805_132451/`. Local checks: assignment/product-board backend 25 passed; frontend productBoard/importResults 10 passed. All core production containers were healthy after worker/scheduler restart.

2026-08-05 13:32 Asia/Shanghai timezone fix: frontend datetime display now formats UTC backend timestamps with `Asia/Shanghai` via `frontend/src/dateTime.ts`. The assignment-board time `2026-08-05 05:16` should display as Beijing time `2026-08-05 13:16`. Production dist backup `/opt/hengzhe-new-product/app/backups/frontend_beijing_time_20260805_133240/`; active frontend asset `index-CjqhJSdG.js`; production health OK. Read-only notification audit confirmed the 13:16 Beijing assignment created/sent 14 operator cards (`新品待办`) to 冯卓宏、刘新雨、姚永运、庞莹莹、李干、李桂敏、江琴、潘露英、赵钰婷、郑纯华、陆伟豪、陆俊全、陈丽妹、饶培丽, all `sent`.

2026-08-04 13:44 Asia/Shanghai production was fully overwritten from the current development environment because the user confirmed development is the desired version. Correct source: `hermes-remote:/opt/hengzhe-new-product-dev/app`; correct production target: `hz-new-product-preprod:/opt/hengzhe-new-product/app`. Production backup before overwrite: `/opt/hengzhe-new-product/app/backups/prod_before_dev_full_sync_20260804_132551/` (DB dump, env, compose, app, outputs, and three business volumes). Development snapshot copied into production: `/opt/hengzhe-new-product/app/backups/dev_full_sync_incoming_20260804_132323/`, verified with SHA256. Apply directory: `/opt/hengzhe-new-product/app/backups/prod_apply_dev_full_sync_20260804_134445/`. Overwritten: Postgres business DB, app source, frontend source/build, `outputs/`, Docker volumes `uploaded-sources`, `source-workbooks`, and `plm-cache`. Preserved: production `.env`, `PLATFORM_BASE_URL=http://101.132.26.138:8080`, `DINGTALK_CARD_AUTOSEND_ENABLED=false`, `PLM_SYNC_ENABLED=false`, reverse proxy and production port. Production services were rebuilt/recreated; final read-back matches development counts: opportunities 5,212; claims 6,609; flow_tasks 9; arrival_records 574; plm_items 576; listings 659; listing_bindings 662; observation_periods 869; notifications 86; users 24. Production health OK, homepage 200, protected API unauthenticated 401, no production DingTalk send.

2026-08-04 13:55 Asia/Shanghai production 502 after the full development sync was traced to the reverse proxy container still running from before the API/frontend recreation. Caddy temporarily logged `lookup api ... no such host` while the API service name was unavailable during recreation. Under production `global.lock`, `reverse-proxy` was force-recreated; no database rows, env files, or DingTalk settings were changed. Read-back after the restart: public `http://101.132.26.138:8080/api/health` returns 200, homepage returns 200 with `/assets/index-BQsRKuqZ.js` and `/assets/index-D4glRYYC.css`, authenticated `/api/dashboard/counts` returns 200, authenticated `/api/product-board` returns 200 with data, and the last 3 minutes of reverse-proxy logs contain no new 502. Future full service recreation must include the reverse proxy, or at least reload/recreate it after API/frontend are stable.

2026-08-04 15:43 Asia/Shanghai follow-up production/development cleanup and listing-image fix are complete. Deleted only test operator configuration residue for `UAT运营A3830/B3830/C3830` in both environments: 3 operator profiles, 4 notification logs, and 16 audit logs per environment; business references were 0, and no product/claim/listing rows were deleted. Backups: production `/opt/hengzhe-new-product/app/backups/uat_operator_cleanup_20260804_153448/`; development `/opt/hengzhe-new-product-dev/app/backups/uat_operator_cleanup_20260804_153518/`. Listing/observation API now returns product images from source claims, explicit listing bindings, or same `main_sku + normalized site/country` product-board opportunities; frontend falls back to listing-row image and normalizes it through `productThumbSrc`. Production deploy backups: `/opt/hengzhe-new-product/app/backups/listing_image_fix_20260804_153716/` and `/opt/hengzhe-new-product/app/backups/listing_image_fix_20260804_154055/`; development source sync backup `/opt/hengzhe-new-product-dev/app/backups/listing_image_fix_20260804_154136/`. Production read-back: listing records 658, image-bearing listing records 459, observation rows 865, image-bearing observation rows 605, remaining no-image listings 199 with same-SKU/country image fallback available 0, API health OK, UAT profiles 0. Development source files were synced but not rebuilt, and the development Compose stack is intentionally stopped (`docker compose ps -a` shows api/frontend/postgres/redis/reverse-proxy/scheduler/worker exited). Do not report the stopped development site as a runtime failure unless the user asks to restart it.

2026-08-04 16:55 Asia/Shanghai production follow-up is complete for the business-period filter hotfix. The operator-claim/workbench business-period dropdown no longer has or auto-applies a `最新期数` option; default remains `全部期数`, and stale selected periods are not silently replaced by the newest period. Local verification: `node --test tests/operatorClaimFilters.test.ts`, full frontend `npm test -- --run` (`234 passed`), and `npm run build` all passed. Production target is `hz-new-product-preprod:/opt/hengzhe-new-product/app`, not `hermes-remote`. Production patch/rebuild was applied under `/opt/hengzhe-new-product/.codex_locks/global.lock` after the PLM import released the lock. Production backups: `/opt/hengzhe-new-product/app/backups/code_pre_latest_period_filter_.tgz` and `/opt/hengzhe-new-product/app/backups/frontend_dist_pre_no_latest_period_20260804_170204.tgz`; production read-back: API health OK, `frontend` container rebuilt and running, and the served container bundle contains no `latestBusinessPeriod` or `最新期数`.

2026-08-04 16:55 Asia/Shanghai production PLM 2026-07-27 through 2026-08-03 import job finished before the frontend hotfix. Job log: `/opt/hengzhe-new-product/logs/new_product_plm_20260804_163200.log`; pre-run DB backup: `/opt/hengzhe-new-product/app/backups/db_pre_plm_retry2_20260804_163200.dump` with SHA file beside it. Use the job log's eight `batch_id` values for this run because production already contains older same-date PLM batches. This run processed 11,752 PLM rows, identified 61 `new_arrival` rows, and created 8 matched arrival/secondary responsibilities: 2026-07-28 1, 07-29 1, 07-30 1, 07-31 1, 08-02 4; 07-27, 08-01, and 08-03 created 0. `DINGTALK_CARD_AUTOSEND_ENABLED` remained false; no production DingTalk card was sent by this job.

2026-08-04 17:50 Asia/Shanghai final production alignment supersedes the 13:44 snapshot counts. A fresh current-development DB dump was created at `/opt/hengzhe-new-product-dev/app/backups/dev_to_prod_current_db_20260804_175017/` and copied to production incoming `/opt/hengzhe-new-product/app/backups/dev_current_db_incoming_20260804_175017/` with SHA256 OK. Production was backed up before restore at `/opt/hengzhe-new-product/app/backups/prod_before_current_dev_db_restore_20260804_175017/`; current production password hashes were saved and restored from `/opt/hengzhe-new-product/app/backups/prod_password_current_20260804_175017/`. Current development app source was archived at `/opt/hengzhe-new-product-dev/app/backups/dev_to_prod_current_app_20260804_175017/` and applied to production after backup `/opt/hengzhe-new-product/app/backups/prod_before_current_dev_app_sync_20260804_175017/`. Production health is OK from both localhost and public `http://101.132.26.138:8080/api/health`; unauthenticated protected APIs return 401; recent API/worker/scheduler logs show no Traceback/Exception/Error/Critical. Verification after the sync: backend key workflow tests `131 passed`, frontend tests `234 passed`, and `npm run build` passed. No PLM/FineBI heavy download was run and no production DingTalk card was sent during this final alignment; the later PLM/FineBI manual run and DingTalk test cards are recorded above.

All 4,837 current development products now store Chinese `country` and `site`: 菲律宾 2,903、泰国 1,127、越南 804、马来西亚 3. Matching code may normalize these values internally, but the current product data and frontend filters must remain Chinese.

The 2026-07-31 upstream ledger remains applied in development: 452 historical PLM arrival facts and 2,026 historical secondary submissions. After the 2026-08-03 PLM 7/27-8/2 top-up, development has 513 `arrival_record` rows and 513 `plm_arrival_item` rows. The 2026-08-03 downstream listing/FineBI layer was rebuilt in development with the “latest data-bearing FineBI weeks” rule: 658 `history_finebi` listings, 660 SKU bindings, and 865 unique `shop+Item+period` observation rows. There are no synthetic `pending_data` periods; 320 zero-data bindings create listing/binding records only. Historical/top-up writes created no FlowTask and sent no DingTalk card.

The 2026-08-03 push/listing rebuild supersedes the older 101-listing/404-period scope. All 666 push-table rows with shop+Item are treated as historical listing evidence, including product-board-missing rows, country-mismatch rows, owner-mismatch rows, and blocked-positioning rows. Market `AQ=利润率/利润` normalizes to `利润款`, `AQ=淘汰` to `淘汰款`, and blank AQ remains blank. Push/listing-source product positioning blanks also remain blank; 淘汰/清仓 rows that enter observation from push evidence have product positioning cleared and must not receive the UI secondary-positioning default such as `稳定款`. The obsolete `1,299 / 68 / 13 / 657` result must not be reused.

Development now also supports manual secondary-research SKU creation from the secondary-research workbench. It creates or reuses the exact `country + main SKU + child SKU + owner` pending-secondary record with source marker `manual_secondary`, but does not create FlowTask rows, DingTalk notifications, PLM facts, or fake upstream stocking evidence. The listing/observation workbench no longer exposes a “含历史档案” switch or history label; it always loads the past/current listing-observation rows by passing `include_history=true`.

2026-08-03 corrected PLM top-up: the earlier Group 1 PLM read-back is invalid for this project and must not be reused. Correct source handling follows `E:\Project\Hengzhe-New-Product-Workflow\PLM系统及相关接口.txt`: log in with the development PLM account `练玉君` (`groupManaye=集团八部`), call `exportSummaryExcel` with the page payload shape and `latestStorageTimeStart/End`, do not pass `blocNameList` or `groupNameList`, then filter the downloaded Excel rows by the `集团` column. Source files for 2026-07-27 through 2026-08-02 were downloaded under `.codex_locks/group8.lock` to `/home/lxc/HengzheProjects/data/group8_new_product_plm_doc_payload_20260727_20260802_20260803/`. The unfiltered files contain multiple groups; the authoritative project subset is `集团=集团八部`. That subset has 61 unique PLM new-arrival events: 53 exact matches to current development `国家+主SKU+子SKU+系统负责人`, and 8 absent from the current product-board key. Development was backed up and then top-up applied under `.codex_locks/global.lock`: the 53 exact matches only received PLM arrival facts, while the 8 board-missing owner rows created 7 `plm_arrival_discovery` opportunities and 8 pending secondary-research records. `GFBI3246/GFBI3246-A4` is one product with two PLM owner rows (`李桂敏` and `潘露英`), so it creates one opportunity and two pending secondary records. Development read-back: `arrival_record=513`, `plm_arrival_item=513`, pending unsubmitted secondary records `22`, of which `8` have `claim_source=plm_arrival_discovery`; `flow_task=0`. Backup/report directory: `/opt/hengzhe-new-product-dev/app/backups/plm_discovery_secondary_20260803_171538/`. Production, DingTalk, restart, and deployment were not touched. Correct coverage outputs: `outputs/plm_group8_docpayload_20260727_20260802_coverage_20260803/`. The apply script used the corrected 61-row coverage CSV, preserving source file and PLM fields; source row is blank for these 61 top-up rows because the raw Excel parse was not used during the write. A follow-up development-only repair fixed coverage-CSV time parsing for non-zero-padded strings like `2026/7/28 16:31`: all 61 top-up `plm_arrival_item.latest_storage_time`, `first_listing_time`, `arrival_record.arrived_at`, and claim `arrival_detected_at` are now populated; repair backup/report directory is `/opt/hengzhe-new-product-dev/app/backups/plm_discovery_time_repair_20260803_1734/`.
For those 53 exact PLM matches, any “secondary research submitted” status comes from historical secondary-research fact rows created by the 2026-07-31 historical market/secondary backfill, not from live task creation. Development `flow_task` remains 0, and the backfill audit explicitly recorded `current_task_state_changed=false`; future reports should label this as `历史二调事实已提交`, not `已生成/已完成任务`.

## Confirmed Business Contract

### Roles and allocation

- The only product roles are `operator` (运营), `manager` (主管), and `super_admin` (超级管理员). The `sales` role option has been removed from admin role choices; production has no `sales` role mapping that needs migration.
- Liu Xuecheng is the sole super administrator; Lian Yujun is the sole manager. Assignment candidates are enabled operators only.
- An operator may configure up to six first-level category groups. Selection1 now supports `category_level2`; assignment recommendation ranks `一级+二级` match first, then first-level match, then all enabled operators by current load. Main-SKU groups stay together within the same source/batch/site.

### Period and source identity

- Selection1 normal periods use `开发MMDD期`; `开发新品0623期` normalizes to `开发0623期`.
- New selection1 standard uploads can include `二级类目`; old historical rows without second-level category must remain blank.
- The development-finance-team source is exactly `开发0727期-财根`.
- Selection2 periods use the independent namespace `选品2-财根MMDD期`; do not merge them with selection1 merely because SKU values match. The standard selection2 shape maps `SPU` to main SKU, `SKU` to child SKU, fixes site/country to `PH`, and leaves categories blank.
- Historical identity is at least: `source namespace + normalized period + country/site + main SKU + child SKU`. Same main SKU with different child SKUs is normal product-specification structure. Same SKU in different countries/sites is not a duplicate.

### Historical selection1 claim recovery

- Scope: `开发0414期` through `开发0721期`. Do not import old periods before 0414. `开发0727期-财根` waits for the separately standardized business source and is excluded from this batch.
- Parse every period through `PERIOD_CLAIM_LAYOUTS` and verify the real headers; each period uses different claim columns.
- Positive numeric claimed daily sales means `认领`. Explicit no, a rejection reason, text entered in the claimed-sales cell, or a blank / `0` daily-sales cell with a filled salesperson means `不认领`; blank/`0` reasons are recorded as `来源表未填写认领单销` / `来源表认领单销为0`. No salesperson means no relationship. `history_selection1` is source/audit provenance only and is never shown as part of the result label.
- Persist country/site, main/child SKU, period, claimant, claimed sales or rejection reason, feedback, remark, source file/sheet/row/column, and attachments. Preserve unresolvable names as evidence but do not create future tasks for them.
- Historical source facts must not overwrite current `platform` claims and must not be represented as a completed manager review.

### Owner and PLM policy

- The claim shown in the current frontend-visible system scope establishes the **system-confirmed owner** for downstream work.
- A PLM row proves arrival for that owner only when `canonical country/site + main SKU + child SKU + PLM salesperson` exactly equals the current system claim, including the same salesperson name. PLM rows for other salespeople do not prove this owner's arrival and must not create this owner's task.
- The system claim remains the ownership authority: exact PLM salesperson matching is an evidence gate, never an owner overwrite or reassignment. Multiple PLM salespeople on one SKU are audited separately; only the row for the system owner participates in that owner's arrival decision.
- Country aliases normalize before matching: `菲律宾/PH -> PH`, `泰国/TH -> TH`, `越南/VN -> VN`, `马来西亚/MY -> MY`. Missing or non-exact main SKU, child SKU, country, or salesperson returns unmatched rather than using a fuzzy fallback.

### Arrival, secondary research, listing, and observation

- A new-arrival candidate needs PLM `AL 最后一次入库时间` and `AM 首次上架时间` on the same Beijing natural day, ignoring time of day. Restocks and either date missing are not new arrivals; do not replace AM with a fabricated “首次入库时间”.
- A future secondary-research branch requires a valid system owner, an effective historical claim, no effective listing Item, no historical secondary-research/market-monitor evidence, and no prior notification for the same arrival batch.
- Historical recovery may populate the existing secondary “我已提交”, pending-listing, and listing/observation views as historical rows, but must not create live FlowTask rows or send DingTalk cards.
- Operators may manually add a missing child SKU into secondary research when the SKU is not yet present in the current platform flow. The manual path is an explicit current operator action, not historical recovery; it must be source-marked and audited, and must not backfill PLM/stocking evidence.
- FineBI is the source of `country namespace + shop + Item + period` metric facts. One shop+Item may legally bind multiple main/child SKUs; metrics are one Item summary and must never be duplicated to every SKU.
- Secondary research requires review conclusion, product positioning, target daily sales, and selling points. `淘汰款` and `清仓款` do not enter listing; only 淘汰款 notifies the manager.

## Data Source Contract

| Source | Authority | Current use |
|---|---|---|
| Selection1 historical workbooks | historical product and claim/rejection facts | restore traceable historical records |
| Selection2 historical workbook | independent finance-team source | one-off import and malformed-owner repair completed in development and production |
| Selection3/4 historical workbook | selection1-shaped historical product and claim facts; Sheet name is period | one-off read-only import completed in development and production; excluded from public pool |
| PLM Excel export | arrival date, inventory, country/site, salesperson | arrival evidence only when its country/main SKU/child SKU/salesperson exactly matches the current system claim; no owner overwrite |
| Market-monitor workbook | historical secondary-research evidence | proves history; missing fields remain empty |
| Listing-monitor workbook | historical shop/Item/operation evidence | proves listing evidence; no metric inference from blank cells |
| FineBI Item finance raw data | shop+Item+period listing evidence and weekly metrics | Item-level summary only |

Known inputs and reports:

- PLM Excel history is available read-only at `C:/Users/86173/.codex/worktrees/7fb3/Hengzhe-New-Product-Workflow/outputs/historical_data/plm_excel_cache/`: 117 daily files covering `2026-04-01` through `2026-07-26`. This rebuild reused that cache and did not download PLM again.
- FineBI raw Item files are present on the active development host under `/opt/hengzhe-new-product-dev/outputs/historical_data/finebi_live/`, covering 17 Thursday-Wednesday periods from `0402-0408` through `0723-0729`; those files were captured on 2026-07-26. The historical-directory copy of `0723-0729` was taken before that week ended and is reference-only. Use the completed rerun `/home/lxc/HengzheProjects/output/group8_weekly_runs/0723_0729_20260730_074802/downloads/finebi_live/05_ItemID财务数据八部_0723-0729.xlsx` with its `itemid_manifest.json` (`row_count=19495`, SHA-256 `cb1eb0b1...294259d`) for that period.
- The completed `0723-0729` rerun is also copied locally to `E:/Project/Hengzhe-New-Product-Workflow/FineBI Item原始数据-0723-0729完整rerun.xlsx`. FineBI Item data has no standalone country column; historical metrics match exact `shop + Item + Thursday-Wednesday period`, while country is validated from the system/listing evidence rather than guessed from compound shop suffixes.
- Historical normalization and read-back outputs live under `outputs/historical_normalization/20260728/`.
- 2026-07-29 canonical rerun uses `选品1：7.27新海外仓开发部门开发新品认领-反馈.xlsx` (SHA256 `724a393e...cc696de`), writes `outputs/historical_normalization/20260729/canonical/`, and is limited to `开发0414期` through `开发0721期`. It has 3,862 raw rows, 3,831 normalized archive rows, 1,621 `认领` facts, 2,209 `不认领` facts. The raw canonical output still contains 14 unresolved groups / 31 source rows, but these must be overlaid with `E:/Project/Hengzhe-New-Product-Workflow/选品1历史清洗与业务核对包-20260728.xlsx`; after applying that business review, all 14 non-finance groups are covered and remaining business-confirmation groups are 0. `开发0727期-财根` is excluded. The local package itself does not write OSS, cards, tasks, or production.
- 2026-07-29 selection1 claim parsing now inherits a salesperson from the same `business period + country/site + main SKU` group when the current row has claim/rejection evidence but its salesperson cell is blank, and only if the group has exactly one salesperson. Example: `开发0526期 / TH / LJJ926 / LJJ926A5 / row 254` inherits `庞莹莹` and becomes `不认领` with reject reason `市场需求量过小`. The rerun output is `outputs/historical_normalization/20260729/canonical_with_group_inheritance/`: 3,831 non-finance archive rows, 3,831 claim/rejection facts, 0 missing relation keys, 1,621 `认领`, 2,210 `不认领`.
- 2026-07-29 clean Selection1 environment-sync package is `outputs/historical_normalization/20260729/selection1_environment_sync/import_rows.json`: 3,977 rows, replacing `开发0414期` with `E:/soft/dingding/历史选品1表财根周期拆分以及开发0414期.xlsx` and adding finance split periods `开发0624期-财根`, `开发0701期-财根`, `开发0708期-财根`, `开发0715期-财根`. `开发0727期-财根` is intentionally excluded until business provides the new normalized source.
- 2026-07-29 development and production were both synchronized with that clean Selection1 package. Runtime used a one-off container script, not a full service deployment, and did not create secondary research, listing, observation, PLM, FlowTask, or DingTalk records. Visible Selection1 opportunities read back as exactly 3,977 rows across the 18 clean periods. Backups: development `/opt/hengzhe-new-product-dev/app/backups/selection1_clean_sync_20260729/`; production `/opt/hengzhe-new-product/app/backups/selection1_clean_sync_20260729/`.
- 2026-07-29 image backfill for the same authoritative workbook uploaded 220 embedded product images to OSS and backfilled both environments with `created=0`, `backfilled_existing_archive=220`. Read-back for `开发0414期` plus the four finance split periods shows all 220 visible archive rows have images. Claim evidence images from feedback/remark cells used a small extracted bundle: development and production each attached 4 new images, found 2 already attached, and had 0 failed uploads. Backup folders: development `/opt/hengzhe-new-product-dev/app/backups/selection1_clean_images_20260729/`; production `/opt/hengzhe-new-product/app/backups/selection1_clean_images_20260729/`.
- PLM/FineBI classification outputs live under `outputs/historical_data/`.
- The canonical latest selection1 import template is now `E:/Project/Hengzhe-New-Product-Workflow/模板/7.29更新版选品1标准表头.xlsx`: it has one standard header row with 83 columns `A:CE`, adding `E 二级类目`. If a source period lacks second-level category, keep it blank rather than guessing.
- 2026-08-03 standardized the补齐 source `E:/Project/Hengzhe-New-Product-Workflow/补齐0728期数据选品1：海外仓开发部门开发新品认领-反馈.xlsx` / side CSV `E:/Project/Hengzhe-New-Product-Workflow/选品1：0728期海外仓开发部门开发新品认领-反馈.csv` as business period `开发0728期`; `7.29更新版` is only the template version, not the data period. Output review package: `outputs/selection1_0728_standardization_20260803/选品1-开发0728期-7.29标准清洗核对包-20260803.xlsx`. Counts after user correction: 426 parsed source rows, 364 clean standard rows, 62 excluded blank/header/non-target rows, 0 duplicate `国家+主SKU+子SKU`, remaining claim anomalies 0. User confirmed `HXG69` four child SKUs (`HXG69RD/GY/BK/GD`) are `不认领`; clean rows set `是否认领=否`, clear `认领单销`, and preserve source reject reason `市场竞对过多，优势不明显`. Source Excel error values such as REF errors are blanked in the clean data and listed separately for audit.
- 2026-08-03 development import for `开发0728期` completed under `.codex_locks/global.lock`. Local package `outputs/selection1_0728_standardization_20260803/selection1_0728_dev_import_package.zip` was rebuilt with `--apply`, `--require-oss`, and environment safety checks; SHA-256 `905bf2f0e52d0c92fb56f42ed3fbacb43418ec9a39512f409b775df4e1c7550c`, 364 rows and 364 images. Development backup: `/opt/hengzhe-new-product-dev/backups/selection1_0728_preapply_20260803_2048/dev_before_selection1_0728.dump`, SHA-256 `523da1aa50e58aba8e9c3d02fd87075240ce25ab1b7278c7484cec341a10ae40`, `pg_restore -l` verified. Dry-run was `would_create=364, would_update=0`; apply read-back is 364 opportunities, 364 claims, 126 `认领`, 238 `不认领`, 364 HTTP/OSS images, no current FlowTask. HXG69 four child SKUs remain `不认领`. Temporary remote package/staging directories were removed after import.
- 2026-08-03 production replay for the same `开发0728期` package is blocked: `/opt/hengzhe-new-product/app` was not found on `hermes-remote`; only `/opt/hengzhe-new-product-dev/app` was confirmed. Do not clone the development database to production. Production needs the real production app path or an explicit instruction to create/deploy a production stack, then backup + dry-run + same package replay.
- 2026-08-03 notification preflight remains non-sending. Development env has `DINGTALK_CARD_AUTOSEND_ENABLED=false`, test receiver configured as `刘学城`, and test recipient userId present. Read-only grouping found 27 arrival notification owner/date groups for `2026-07-27` through `2026-08-02`; no DingTalk card was sent in this pass. Local focused notification/claim regression passed `70 passed`. A root-cause fix for returned-supplement claim reuse is present in local `backend/app/services.py`, but the development container read-back still reports that fix is not deployed; do not run real notification/send tests until code is deployed or explicitly accepted.
- 2026-08-03 remote resource incident note: the server was not disk-full (`/dev/vda3` 17% used, memory about 11Gi available). High load during the session came from an unrelated `group1_shop_daily/sync_group1_small_goods_listing_stats.py --skip-full-upload` task plus the independent image platform. New-product containers were low CPU after the import. Future new-product writes must keep using `.codex_locks/global.lock`, dry-run first, one data source at a time, and avoid running heavy PLM/FineBI downloads or full test suites on the remote host during other production jobs.
- 2026-08-03 PLM discovery product-board bug root cause: the one-off development script `apply_dev_plm_secondary_20260803.py` wrote PLM-discovered products with `developer_name=<PLM salesperson>` and `current_status=claim_submitted`, while the product-board query only joined responsibility claims from `platform`, `history_selection1`, `history_selection2`, and `history_selection34`. Result: detail showed the PLM salesperson as “开发员”, but the board did not show them under “已分配运营”. Local fix adds `plm_arrival_discovery` and `manual_secondary` responsibility claims to the board query and a regression test for `CAHG224`; `backend/tests/test_product_board.py -q` passed `11 passed`. Development deployment/data cleanup is not done because SSH timed out during banner exchange. When SSH recovers, patch the development API, then clear `NewProductOpportunity.developer_name` only for `source_type='plm_arrival_discovery'` after backup and dry-run; keep the operator in `SalesClaimForecast.salesperson_name`.

## Downstream Reset and Re-entry Gate

- Development backup: `/opt/hengzhe-new-product-dev/app/backups/downstream-reset-20260731_120724/workflow_dev_before_downstream_reset.dump`.
- Production backup: `/opt/hengzhe-new-product/app/backups/downstream-reset-20260731_120842/workflow_prod_before_downstream_reset.dump`.
- Both custom dumps passed SHA-256 and `pg_restore -l` verification. The development reset removed 1,192 listings, 1,292 bindings, 2,176 observation periods, and cleared 2,445 secondary-research rows. Production removed 1,370 listings, 1,501 bindings, 2,423 observation periods, and cleared 2,714 secondary-research rows.
- Re-entry must use an immutable staging/evidence pass first. Classify rows as exact, ambiguous, conflicting, or missing; only exact rows may advance automatically.
- Historical secondary, listing, and week evidence may populate the existing workbenches as historical submitted/listing/observation rows for exact current-system matches; it must stay distinguishable from live tasks and must not generate FlowTask or notifications.
- Authority order remains: source workbook for historical secondary fields; listing-monitor workbook for shop/Item evidence; FineBI for Item validation and weekly metrics; PLM for arrival fact only; system-confirmed claimant for ownership. Mismatches go to review and never auto-overwrite an owner.
- Historical imports create no FlowTask, review, stocking task, or DingTalk card. Current automatic tasks may be created only by the separately approved current-arrival flow.
- 2026-07-31 development rebuild cleanup is complete. Backup: `/opt/hengzhe-new-product-dev/backups/history_rebuild_20260731_160000/dev_before_history_rebuild_20260731_160000.dump` (SHA-256 `41625a2e...71aeb`, `pg_restore -l` passed). Read-back: duplicate responsibility facts `0`, `arrival_record=0`, arrival flags `0`, listing/observation `0`, and current downstream task-state rows `0`. Production was not changed.
- The cleaned market source has 2,041 unique `country+owner+main SKU+child SKU` rows (SHA-256 `1b4eab17...f67ce`). Final eligibility is 2,026 exact submitted rows after excluding 12 rejected-claim rows and 3 `PH/HWCJC196/HWCJC196A1` owner conflicts; 391 rows contain at least one real AM:AQ value. No blank value was synthesized; AL was ignored. `利润率`/`利润` normalize to `利润款`; `淘汰` normalizes to `淘汰款`.
- `LHMO1285` is present and matches normally. The prior “657 missing” result was invalid because it only queried a narrowed Selection1-period scope instead of the frontend-visible all-source claim scope.
- For listing observation, the four-week window means the latest up to four Thursday-Wednesday FineBI periods that actually contain a raw row for that exact shop+Item across the 17-week cache, not a fixed calendar window and not the first four weeks after operation/listing date. Listing workbook column A `操作日期` is source trace only. A FineBI row with zero orders or zero revenue still counts as a real data week; no raw row means no observation period is created.
- Business role split confirmed on 2026-08-03: arrival notifications and secondary-research tasks belong only to the platform claimant. Non-claimants may still list/sell another claimant's product; therefore push-workbook owner mismatches are not automatically listing conflicts. Store the push-sheet owner as listing/seller evidence while preserving the platform claimant for arrival/secondary ownership.
- The 2026-07-31 18:49 push-workbook coverage audit is read-only: 666 source rows, 492 exact current-platform rows, 47 owner mismatches, 95 product-board misses, 1 country mismatch, and 31 rows without an effective claim. Two of the 492 exact rows match multiple platform periods and require a period decision. One same-owner shop+Item group is a legal shared binding across two main SKUs; cross-owner Item conflicts are 0. Review package: `outputs/019fb6ab-d58e-7e90-ba4c-f21474dc971d/finebi_item_first_week_audit/推品表未命中业务核对清单-20260731.xlsx`. No database row was changed by this audit.
- 2026-08-03 repair outputs under `outputs/019fb6ab-d58e-7e90-ba4c-f21474dc971d/latest_four_repair_20260803/` supersede the old push/listing staging counts. Under the latest confirmed historical-cleanup rule, all 666 push rows may enter listing observation as 660 unique `shop+Item+mainSKU` bindings. The former blockers are now writable as history: 95 product-board-missing rows enter by push-sheet shop+Item, 12 淘汰/清仓 rows enter with product positioning cleared, 1 country mismatch enters by push-sheet country, and 1 invalid push owner is corrected to the platform claimant. The rebuilt plan selects the latest up to four FineBI periods with raw rows per shop+Item: 872 binding-view data weeks, deduped to 865 unique database observation periods because shared Items store metrics once. Distribution by binding is 320 with 0 data weeks, 101 with 1, 62 with 2, 61 with 3, and 116 with 4.
- 2026-08-03 user resolved one remaining multi-period audit row: push row 442 (`菲律宾 / YZH007 / 陈丽妹 / Shopee-311PH / Item 54013530289`) must attach to `开发0526期`. `选品2-财根0526期` is retained only as an unselected candidate period for audit and must not be deleted from the system.
- 2026-08-03 development database rebuild completed under `global.lock` and production remains untouched. Backup/report directory: `/opt/hengzhe-new-product-dev/backups/history_latest_data_weeks_20260803_140657/`. Read-back: `history_finebi` listings 658, bindings 660, observation periods 865, `history_push_20260727` listings 0, platform observation periods 0, pending-data periods 0, FlowTask 0, notifications 54. Row 442 reads back on `开发0526期` with one period `2026-07-16` to `2026-07-22`.
- 2026-08-03 development deploy completed under `global.lock` for manual secondary SKU creation and listing/observation default history inclusion. Backup `/opt/hengzhe-new-product-dev/app/backups/manual_secondary_listing_history_20260803_144505/`; deploy log `/opt/hengzhe-new-product-dev/app/logs/deploy_manual_secondary_20260803_144505.log`. Read-back: API health OK, unauthenticated `/api/secondary-research/manual` returns 401 rather than 404/405, frontend bundle has `新增二调 SKU` and no `含历史档案`.
- 2026-08-04 development hotfix completed under `global.lock` for PLM discovery ownership display and assignment-table match coloring. Backup `/opt/hengzhe-new-product-dev/app/backups/assignment_plm_display_20260804_093333/` includes the PostgreSQL custom dump, API file backup, frontend source backup, static backup, and build log. API now includes `plm_arrival_discovery` and `manual_secondary` responsibility facts in the product board; 7 PLM-discovery opportunities had `developer_name` cleared and `current_status` moved to `waiting_secondary_research`. Read-back: `CAHG224` shows responsibility `赵钰婷`, `CJA2192` shows `李桂敏`, and PLM-discovery rows with developer names are `0`. Frontend bundle is `/assets/index-BTTd7H2f.js`; the assignment table now colors final-assignee site/category matches green and misses red. This PLM display/assignment-table change remains development-only; production later received only the DingTalk card URL hotfix recorded above.
- 2026-08-04 7/27-8/3 PLM package was downloaded locally first, then uploaded to development. Local package: `outputs/local_plm_download_20260804/plm_20260727_20260803_local_package.zip`, SHA-256 `a5dc43ffd0941db4f1467b84cb3034fddf518ff9d404b0872d4b311d9c0ceca8`; remote package: `/opt/hengzhe-new-product-dev/app/outputs/local_ingest/plm_20260727_20260803_20260804/`. Correct PLM source is normal Excel export with `集团八部`, not the failed open JSON endpoint. Downloaded 8 days, 12,110 rows, 62 `new_arrival` rows. Development apply ran under `global.lock` after backup `/opt/hengzhe-new-product-dev/app/backups/dev_plm_local_ingest_20260804_101719.dump`: created 59 arrival records, 8 PLM batches, 59 PLM items, 1 `plm_arrival_discovery` secondary task placeholder, updated 50 submitted claims to `historical_secondary_submitted`, deleted 44 empty duplicate `selection1_tail` claims, and skipped 3 true multi-period conflicts. Development code hotfix backup `/opt/hengzhe-new-product-dev/app/backups/dev_plm_hotfix_20260804_102035`; current frontend bundle `/assets/index-C4s8kBCE.js`. Read-back: API healthy, `historical_secondary_submitted=50`, package arrival records `59`, PLM discovery `1`, product board service filters 24 groups with that status. Production was not changed.
- 2026-08-04 production and development cleanup completed under each environment's global lock. PLM discovery must not create assignment tasks; assignment table is only for standard selection imports. Deleted test-only `UAT0804全链路期` and erroneous `PLM到货20260804` records from both environments, renamed real PLM periods to `PLM新品到货20260727-0802` and `PLM新品到货20260803`, and fixed 920 historical selection1 reject-only opportunities from `pending_assignment` / `claim_rejected` to `已确认不认领`. Backups: production `/opt/hengzhe-new-product/app/backups/period_cleanup_20260804_143000/`; development `/opt/hengzhe-new-product-dev/app/backups/period_cleanup_20260804_143054/`. Read-back in both environments: old periods count `0`, new PLM periods `7` and `1`; HCK023PK/DG/GN are 江琴 `已确认不认领`, HCK023SV remains 江琴 `claim_submitted`. Backend `plm_processing.py` and `plm_local_ingest.py` hashes are identical in production and development (`b666d42e...`, `70e753f...`), and API health is OK.
- 2026-08-04 follow-up cleanup/rename completed under each environment's global lock. Rename PLM discovery batches from `PLM新品到货...` to `PLM到货新增SKU...`; these batches mean “SKU absent from the platform, discovered from PLM arrival and added as pending secondary-research records,” not the full PLM arrival window. Deleted 1450 duplicate `selection1_claim_feedback / selection1_tail` rows that exactly duplicated `history_selection1` owner/result/daily-sales facts and had no secondary fields or downstream references. Backups: production `/opt/hengzhe-new-product/app/backups/selection1_dedupe_plm_rename_20260804_144411/`; development `/opt/hengzhe-new-product-dev/app/backups/selection1_dedupe_plm_rename_20260804_144505/`. Read-back in both environments: `DUP_TAIL_COUNT=0`, HCK023SV has one claim row (`history_selection1`, 江琴, claim, 1.0), `PLM到货新增SKU20260727-0802=7`, `PLM到货新增SKU20260803=1`, old `PLM新品到货...=0`.
- 2026-08-04 follow-up scope correction: user clarified the historical PLM window should be 2026-04-14 through 2026-07-26, while 2026-07-27 through current date is current/new PLM arrival work that may need secondary research. User then explicitly paused rollback/reclassification because prior arrivals may also still be unresearched. Therefore do not assume the 50 `historical_secondary_submitted` rows from the 7/27-8/3 package are final production-ready semantics, and do not keep expanding that conversion without a new confirmed rule. No database change was made for this clarification.
- 2026-08-05 production hotfix: super-admin user delete is now a safe disable, not a physical `users` row deletion. It disables the user, role mappings, and assignment profile while retaining password/history references, preventing the `user_password_user_id_fkey` 500. Assignment personnel config now refreshes operator profiles/assignable operators after user changes and after add/remove/save in the assignment pool; opening the config drawer also triggers a lightweight refresh. Production backup `/opt/hengzhe-new-product/app/backups/user_admin_fix_20260805_1115/`; health/read-back OK.
- 2026-08-05 production frontend hotfix: source workbook import now shows explicit in-card progress while uploading/importing large Excel files: `正在上传并导入，请勿关闭页面。大文件和图片上传可能需要几分钟。` The button is disabled and changes to `正在上传并导入...` to prevent double clicks. Production backup `/opt/hengzhe-new-product/app/backups/source_import_loading_fix_20260805_1130/`; served asset `assets/index-DYRxNono.js` contains the new text and health is OK.
- 2026-08-05 production data correction: user confirmed `开发0804期` rows `HYK034` through `HYK038` are one product whose main SKU must be `HYK034`. After backup `/opt/hengzhe-new-product/app/backups/hyk034_main_sku_fix_20260805_121223/`, four opportunity rows were corrected from `HYK035/HYK036/HYK037/HYK038` to `HYK034` under `/opt/hengzhe-new-product/.codex_locks/new_product_workflow_prod.lock`. Source snapshots were kept unchanged for Excel audit. Read-back: `HYK034_group_count=5`, old main SKU remaining count `0`, all five rows retain HTTP OSS image URLs, production health OK.
- 2026-08-05 production OOM/502 incident: root cause was not a long PostgreSQL query; Caddy logs showed multiple users/refreshes hitting `/api/opportunities?limit=5000` together with dashboard/tasks/config requests, causing the API process to grow and be kernel OOM-killed (`uvicorn`, exit 137). PostgreSQL `pg_stat_activity` showed no long-running active query. Hotfixes applied under `/opt/hengzhe-new-product/.codex_locks/new_product_workflow_prod.lock`: `/api/opportunities` is capped to `limit <= 300`, frontend default refresh now calls `api.opportunities(300)`, and assignment table controls are sticky. Backups: `/opt/hengzhe-new-product/app/backups/hotfix_oom_opportunities_cap_20260805_143619/` and `/opt/hengzhe-new-product/app/backups/hotfix_oom_opportunities_cap300_20260805_144135/`. Worker and scheduler were intentionally stopped during recovery to protect foreground usage; restart them only after confirming memory headroom. Verification: production `/api/health` returned 200 five times, homepage 200, container code read-back `limit: int = Query(300, ge=1, le=300)`, and no new OOM appeared during the final observation window.
- 2026-08-05 follow-up production OOM guard: the product-board "continue loading from server" path was removed because it could grow the backend query from 300 toward 3000 groups and trigger 502 again. Product-board period options now merge full `ImportBatchSummary.business_period/source_sheet` metadata with loaded rows, so the dropdown does not lose periods just because the visible row list is capped. Backend `/api/product-board` is also capped to `limit <= 300` to protect users with stale cached frontend bundles. Current served asset is `/assets/index-Drx7rlv5.js`; it contains `加载更多` and no `继续从服务器加载`. Production memory remains tight (`MemAvailable` around 120MB, swap around 4.5GB used, unreclaimable slab around 983MB); keep worker/scheduler off until memory headroom is restored or the server is resized/cleaned.
- 2026-08-05 production recovery/cleanup: production hit another 502 while Docker/host commands were hanging under memory pressure; a host reboot was requested and service recovered (`/api/health` 200, `MemAvailable` about 2.7GB, swap cleared). Then the user-approved dirty period names were physically removed from production DB scope: `5.26期`, `8.4期`, `latest7.27_cleaned`, `历史全期`, `历史归档`, `历史选品3/4全Sheet`, `后半段UAT-20260724`, `PLM新品到货`, `PLM到货新增SKU20260727-0802`, `PLM到货新增SKU20260803`, `开发0727期-财根`. Backup before deletion: `/opt/hengzhe-new-product/app/backups/workflow_prod_before_period_delete.dump` (22MB). Deleted 28 dirty `import_batch` rows total and 10,772 `source_record_snapshot` rows; 3,219 normal products that referenced the remaining dirty import batches were kept and only had `import_batch_id` cleared. Final read-back: bad-period opportunities `0`, bad import batches `0`, bad batch snapshots `0`, and product-board period service `BAD_PRESENT=[]`. Frontend served asset after period-scope fix is `/assets/index-CSvkcLZv.js`.
- 2026-08-05 production import queue fix: source workbook imports were stuck at `import_job.status=pending` because the production worker had been intentionally stopped after OOM recovery. After starting the worker, the pending `selection1` job failed immediately because the worker service did not mount the API upload volumes (`source-workbooks`, `uploaded-sources`), so it could not read `/app/.private_uploads/source-workbooks/321b5e059bc2451e8ad56f3643f918c7-新品0804期.xlsx`. Fixed `docker-compose.yml` by adding those volumes to `worker`, redeployed only the worker with `--no-deps`, reset the failed job to pending, and it completed. Result: `开发-财根团队7.29-8.4` imported 8 rows (7 created, 1 updated, 0 skipped), import batch `99a29e0a-10a3-451b-86f6-dd467ed629d6`; 5x `/api/health` returned 200 and no recent 502. Resource limits were restored with `docker update`: API 1408m, worker 768m, Postgres 384m, Redis 64m, frontend/reverse-proxy 96m.
- 2026-08-05 assignment visibility follow-up: the imported `开发-财根团队7.29-8.4` rows were in DB as `pending_assignment` (4 main SKU groups: MINA122, WPHG525, WPHG526, YHZ070; 7 sub SKUs), but the page did not show them because source import completion only switched to the assignment tab and did not refresh the global opportunity list used by the pending assignment panel. Fixed `frontend/src/App.tsx` to `await refresh({ silent: true })` after a background import job completes and before switching views. Built frontend asset `assets/index-BrE0WZbP.js` and copied it into the production frontend container `/usr/share/nginx/html`; host and container read-back both serve that asset. Verification: frontend import/product-board tests 12 passed, `npm --prefix frontend run build` passed, production `/api/health` 3x 200.
- 2026-08-05 WPHG526 production row repair: user identified `新品0804期.xlsx` sheet `开发-财根团队7.29-8.4` row 4 (`25*25*25cm`) should be `WPHG526A1`, but the source sheet had row 4 and row 5 both as `WPHG526A2`. The import de-duped by sub SKU, so row 4 was collapsed into row 5 and WPHG526 only had A2/A3. Production repair under lock: DB backup `/opt/hengzhe-new-product/app/backups/wphg526a1_fix_before.dump`; created missing opportunity `2b926258-870a-4e76-b66e-834d510b7fb7` as `WPHG526A1`, source row 4, spec `25*25*25cm`, copied same assignment state as WPHG526A2/A3 and created pending sales-claim task for assignee `李干`; updated source snapshot row 4 payload and opportunity_id to point to WPHG526A1. Read-back: WPHG526 A1/A2/A3 source rows 4/5/6, all `assigned` to 李干 with pending task, `/api/health` 200.
- 2026-08-05 image preview z-index fix: image preview overlays were rendered inside cards/forms, so sticky review bulk bars and card checkboxes could appear above the enlarged image despite `.image-preview` z-index. Fixed `frontend/src/App.tsx` by adding a shared `ImagePreviewPortal` rendered through `createPortal(..., document.body)` and routing ProductThumb, evidence upload preview, and submitted evidence preview through it. Tests: `node --test frontend/tests/importResults.test.ts frontend/tests/reviewLayout.test.ts frontend/tests/productBoard.test.ts` = 30 passed; `npm --prefix frontend run build` passed. Production frontend container updated to `assets/index-DMCmakUn.js`, `/api/health` 200.
- 2026-08-05 product detail sidebar sticky fix: user wanted the left “商品全局档案” navigation to stay visible while scrolling long right-side product detail content. Root cause: `.detail-sidebar` was a stretched CSS grid item, which made sticky unreliable. Fixed `frontend/src/styles.css`: `.detail-workspace { align-items: start; }`, `.detail-sidebar { align-self: start; position: sticky; top: 56px; max-height: calc(100vh - 140px); overflow-y: auto; overscroll-behavior: contain; }`. Tests: `node --test frontend/tests/opportunityDetails.test.ts frontend/tests/reviewLayout.test.ts` = 22 passed; `npm --prefix frontend run build` passed. Production frontend container updated to JS `assets/index-B4B5-MYT.js`, CSS `assets/index-DgabRR7O.css`, `/api/health` 200.
- The 195 hidden **database rows** were not 195 unique SKU identities absent from the product board; some keys also had a separate visible row. After exact reference audit and backup they were physically removed from development. Visible product count remains 4,830 and disabled count is 0.
- Development downstream cleanup completed after explicit user approval. Backup `/opt/hengzhe-new-product-dev/backups/secondary_field_cleanup_20260731_171253/workflow_dev_before_secondary_field_cleanup.dump` passed SHA-256 (`0a16ee08...cb167b`) and `pg_restore -l`; the exact 397 cleared rows are preserved in `cleared_secondary_fields.csv`. Read-back: secondary business fields 0, submitted secondary rows 0, listings/bindings/observations/open tasks 0; opportunities 5,025, claims 6,386, and source snapshots 12,875 were unchanged. Production was not touched.

## Current Implementation Focus

- Development downstream workbenches now contain the rebuilt exact historical rows described above; production remains at the post-reset baseline. Older bullets below describe implementation history unless a later 2026-07-31 read-back supersedes them.
- Historical selection1 parsing and claim recovery: `backend/app/historical_selection1_claim_backfill.py`.
- Historical selection1 import/state restoration: `backend/app/historical_selection1_import.py`, `backend/app/historical_selection1_state_restore.py`.
- Future arrival bridge: `backend/app/historical_arrival_activation.py`, `backend/app/plm_processing.py`, `backend/app/notification_jobs.py`.
- Personnel/role UI and APIs: `backend/app/admin_console.py`, `backend/app/routers/admin.py`, `frontend/src/adminConsole.ts`.
- Development and production product boards display both current `platform` and read-only `history_selection1` claim/rejection relationships in `已分配运营`; this is display-only and must not create tasks or overwrite current claims. On 2026-07-30 production API was hotpatched to match the development product-board and product-detail behavior, and legacy visible `历史归档` rows were disabled.
- BGQHG12 acceptance is verified in both environments: `开发0414期 / BGQHG12` shows `陆俊全` in product-board responsibilities, and each child detail row has one read-only historical claim with `claim_result=reject`, reject reason `市场竞对过多，优势不明显`, and source period/row `开发0414期` rows 6-9. Authority workbook rows 6-9 have no CE remark images, so their detail evidence count is correctly 0.
- Production product-detail hotpatch backups: backend `/opt/hengzhe-new-product/app/backups/product_detail_historical_claims_20260730_110659/`, frontend runtime `/opt/hengzhe-new-product/app/backups/product_detail_historical_claims_frontend_20260730_111029/`. Production evidence DB backup: `/opt/hengzhe-new-product/app/backups/product_detail_historical_claim_evidence_20260730_111357/workflow_prod_before_claim_evidence_20260730_111357.sql.gz`.
- Production claim-evidence bundle from `outputs/historical_normalization/20260730/authority_selection1_claim_evidence_20260730.zip` was applied only to existing `history_selection1` rows: `attached=33`, `already_attached=8`, `failed_uploads=[]`; 37 historical claims now have OSS evidence. Active API sample `开发0414期 / HFL016 / row 2` reads back `evidence_count=1`, source column `CE`.
- Product board period scope is currently clean in both environments: 18 visible periods from `开发0414期` through `开发0721期`, plus the four split finance periods `开发0624期-财根` / `开发0701期-财根` / `开发0708期-财根` / `开发0715期-财根`. `开发0727期-财根`, `历史归档`, and `5.26期` are disabled and must not appear to normal users.
- 2026-07-31 business scope update: only currently visible normalized periods require correctness. The 195 disabled dirty archive rows were physically removed from development after a separate backup and downstream-reference audit; no current visible product was removed.
- Product board frontend mirrors listing observation's light rendering pattern: first 50 main-SKU groups render by default, then "加载更多" appends 50 at a time. The active Hong Kong development runtime is `/assets/index-CXN9BxBx.js`; the former development instance is stopped. Filtering/statistics still use the full loaded dataset.
- 2026-07-30 detail-cache fix: the frontend detail refresh now caches and reattaches `historical_claims` together with `snapshot`; previously the `/opportunities/{id}` API returned history but the refreshed list row kept only `snapshot`, so AZSHG1302A1 showed an empty "认领与复核" table. Development backup `/opt/hengzhe-new-product-dev/app/backups/frontend_detail_history_claim_cache_20260730_114413/`; production backup `/opt/hengzhe-new-product/app/backups/frontend_detail_history_claim_cache_20260730_114455/`.
- 2026-07-30 development data cleanup: removed 90 stale `history_selection1` rows whose `salesperson_name` was literally `是/否/Y/N`; all were unreferenced. Dev backup `/opt/hengzhe-new-product-dev/app/backups/claim_invalid_salesperson_cleanup_20260730_115728/`. Read-back: `开发0715期-财根 / QTWH057D5 / row 23` now has only `陆伟豪` reject; production already had 0 such invalid names. `开发0624期-财根 / YZH040GY / row 17` has no claim record because authoritative BZ:CE cells are empty.
- 2026-07-30 product-board status-label fix: frontend maps internal `historical_archive` to business labels only. Historical claim rows with daily sales display `认领`; historical reject / blank-or-zero daily-sales rows display `不认领`; archive rows with no salesperson relationship display `未认领`. Development backup `/opt/hengzhe-new-product-dev/app/backups/frontend_product_board_status_labels_20260730_120818/`; production backup `/opt/hengzhe-new-product/app/backups/frontend_product_board_status_labels_20260730_120818/`. This was a frontend-only static hotpatch; no database write, task creation, backend restart, or DingTalk card.
- 2026-07-30 selection2 development release: standard A:AP imports create unowned pool records; pool joins persist per operator and allow multiple operators on the same child SKU; reviews and downstream ownership remain claim-specific. Selection2 details use five source blocks, and secondary research reuses the current workflow with selection2 source tabs.
- One-off history source `历史待导入标准选品2：海外仓财根团队开发新品认领-反馈.xlsx` initially imported 249 opportunities, 249 snapshots, 137 claims and 110 rejections across seven normalized periods. All 213 source rows with WPS images have OSS evidence; failed uploads are 0. The 2026-07-31 authority correction changed both environments to 138 claims and 97 rejections. Read-back found no new FlowTask, review, stocking, listing, observation, notification, or scheduler run.
- Development browser acceptance: the public pool filters by seven `选品2-财根MMDD期` periods; historical cards are read-only; X5CKY41C shows one, not duplicated, claim row for 冯卓宏 with daily sales 0.5 and source row 25.
- 2026-07-30 selection3/4 history import: `backend/app/historical_selection34_import.py` consumed the 14-Sheet authority workbook `历史数据--选品3&选品4表.xlsx` and created 596 read-only opportunities, 604 snapshots, and 604 `history_selection34` claim facts in development. All rows have `claim_pool_open=false`; no FlowTask, review, stocking, secondary research, listing, observation, notification, or scheduler record was created. Import batch `2812a96d-a211-4052-888c-0f642dac6575`; backup `/opt/hengzhe-new-product-dev/app/backups/selection34-history-20260730_200303/`.
- The authority workbook contains no product image values, embedded/WPS image parts, or direct image URLs; its AA/AJ/AM links are ordinary competitor product pages and must not be used as product photos. All 169 CE notes were preserved and there is no OSS retry list for this batch. A second dry-run reports `create=0`, with 596/604/604 idempotent updates.
- Development browser acceptance: BGQHG32 board/detail show 陆伟豪 and `不认领`, source reason `来源认领单销为 0`, period `选品2-财根0526期`, row 10. CJA1249D shows 陆俊全 and 陈丽妹 as multiple claimants; detail reads daily sales 4.5/1.31, notes 43531303924/45511621146, and rows 6/46. Detail status labels now distinguish `认领`, `不认领`, `未认领`, and mixed history; top detail tags show historical operators after hydration.
- 2026-07-31 production release: backup `/opt/hengzhe-new-product/app/backups/selection2-34-prod-20260731_100000/` contains the PostgreSQL custom dump, runtime files, input hashes and import reports. Production now has 249 `history_selection2` opportunities with 137 claims, 110 rejections, 249 snapshots and 213 OSS image rows; and 596 `history_selection34` opportunities with 604 claims/snapshots and 0 image rows. Both sources have `claim_pool_open=false`; the existing 20 normal selection2 opportunities were backfilled to `claim_pool_open=true`.
- 2026-07-31 selection2 owner repair: the authority workbook has six rows where the primary salesperson shifted from `AQ` to `AR` and the rejection reason shifted into `AT`, plus one row whose positive daily sales shifted into `AS`. The parser restores those seven relationships, treats any `侵权` marker as archive-only, and rejects numeric/reason text as salesperson names. Development backup `/opt/hengzhe-new-product-dev/app/backups/selection2-invalid-owner-repair-20260731_103436/`; production backup `/opt/hengzhe-new-product/app/backups/selection2-invalid-owner-repair-20260731_104658/`, import batch `0aeab327-0e13-4ac5-9557-d420ba62620f`. Each environment deleted the same 19 unreferenced invalid rows and reads back 138 claims / 97 rejections with no invalid owner options.
- Production read-back for AXBI3166 confirms source `销售自选0601期` row 2, owners 江琴/郑纯华 and 28 non-empty A:CE source fields. The detail frontend now maps selection3/4 N:Z, AA:AO, AP:AW and AX:BY into the existing selection1-style sections and retains all non-empty cells in `源表字段`; BZ:CE remains in `认领与复核`.
- 2026-07-31 selection1 standard detail-section fix: all 138 rows in the four finance split sheets use single-level headers and `source_schema=selection1_standard_v20260729`. Their source snapshots were complete, but semantic header grouping left development and market sections empty and cost fields incomplete. The shared frontend now uses fixed ranges N:Z, AA:AO, AP:AW and AX:BY for this schema while keeping BZ:CE in claims/evidence. GZMO097BK reads source AA/AB/AC as competitor link, price 298 and monthly sales 543. Database read-back confirms both development and production have `marked=138, unmarked=0`. Development backup `/opt/hengzhe-new-product-dev/app/backups/selection1-standard-detail-sections-20260731_110903/`; production backup `/opt/hengzhe-new-product/app/backups/selection1-standard-detail-sections-20260731_111016/`.
- 2026-07-31 frontend API-base incident: the first detail-section hotpatch bundle embedded `http://localhost:8000`, causing browser login to show `Failed to fetch` although `/api/auth/login` itself was healthy. `frontend/src/api.ts` and `frontend/src/imageSource.ts` now default to same-origin `/api`; the production test scans both files against localhost regression. Development and production now serve `/assets/index-CXN9BxBx.js`, which contains no `localhost` or `127.0.0.1`. Development browser login with the configured super-admin account succeeded and remained authenticated after reload.
- No FlowTask, review, stocking, secondary research, listing, observation or notification row was created. `scheduler_job_run` increased only by the normal 10:00 `manager_notification` audit run with zero business actions. Production API is healthy, both environments currently serve `/assets/index-CXN9BxBx.js`, and `DINGTALK_CARD_AUTOSEND_ENABLED=false` / `PLM_SYNC_ENABLED=false`.
- Latest verification: backend focused `65 passed`, frontend full `227 passed`, frontend build, Alembic `a7b8c9d0e123 (head)`, idempotent post-import dry-runs (`create=0`), production DB/static-asset read-back, and `git diff --check` passed.
- Before changing behavior, trace all callers and update the narrow tests. The worktree already contains uncommitted changes in these areas; do not assume they are complete or deployable.
- 2026-08-05 product detail UX: production frontend now keeps the main SKU summary, child-SKU switcher, active child summary, and left archive nav visible while scrolling long detail content. A follow-up tightened the content sticky offset to `164px` and added a white backdrop to prevent source-field rows showing through between sticky blocks. Current production assets are `assets/index-CbHetHHM.js` and `assets/index-BLaTetAs.css`; `/api/health` read-back is 200. This was a frontend-only static deploy under `/opt/hengzhe-new-product/.codex_locks/new_product_workflow_prod.lock`; no database/backend changes.
- 2026-08-06 operator submit UX: `MINA122` production read-back confirmed 冯卓宏's not-claim submission was saved (`claim_rejected`, claim task completed, manager review pending). The confusing toast came from post-submit refresh calling manager-only personnel config as a normal operator. Frontend `refresh()` now loads operator profiles/import batch/admin dictionaries only when `canManage`; production frontend asset is `assets/index-BHy1r1af.js`. No database/backend changes.
- 2026-08-06 manager review UX: product detail now records the entry view and returns to that view via `closeProductDetail()`, so details opened from 主管复核 return to the review list instead of a default workflow page. The review list's type tabs, search/pagination, and bulk approve/reject controls are wrapped in `.review-sticky-controls` and remain visible while scrolling. Production frontend-only deploy under `/opt/hengzhe-new-product/.codex_locks/new_product_workflow_prod.lock`; backup `/opt/hengzhe-new-product/app/backups/frontend_review_sticky_back_20260806_175733`; current assets `assets/index-6ljhyxVJ.js` and `assets/index-BDwfNXeX.css`; `/api/health` 200.
- 2026-08-06 export center UX/data fix: production `开发0804期` read-back has 0 stocking requests and 123 central traceability rows, so the right-side “导出选中” button is expected to be disabled; users should use “导出中央追溯表” for those 123 rows. Backend now returns both pending submitted stocking requests and exported stocking requests (`operation_status=已导出`) in `/stocking/available-list`; period cards keep `待导出` as submitted-only and `追溯` as submitted+exported+not-claim traceability. Frontend shows exported rows but disables their checkbox to prevent repeat export. Production backup `/opt/hengzhe-new-product/app/backups/stocking_export_visible_20260806_180324`; current frontend assets `assets/index-BDrr5OZy.js` and `assets/index-BDwfNXeX.css`; `/api/health` 200.
- 2026-08-06 export center dual-detail view: added `/stocking/traceability-list` and frontend `备货申请明细 / 中央追溯明细` tabs. Stocking view keeps selected-request export; traceability view shows traceability rows (type/status/salesperson/SKU/reason/feedback/review/source) without stocking checkboxes. Production `开发0804期` read-back: 123 traceability rows, all `不认领结果`; central traceability workbook generation was verified at ~46.8MB and ~11s server-side, so the button now shows `导出中…` and a wait message. Production backup `/opt/hengzhe-new-product/app/backups/traceability_list_view_20260806_181428`; current frontend assets `assets/index-8kZoQTBb.js` and `assets/index-BDwfNXeX.css`; `/api/health` 200.
- 2026-08-06 export center review-detail export: `/stocking/traceability/export` was changed from the old full central-field workbook to a lightweight single-sheet `认领复核明细` workbook. Export columns are exactly `站点 / 主SKU / 子SKU / 销售员 / 认领结果 / 认领单销 / 不认领理由 / 图片附件 / 主管复核状态（中文） / 主管复核意见`; both approved claims and confirmed not-claim rows are included. Values are Chinese (`认领`/`不认领`, `认领通过`/`确认不认领`/`退回补充`), product images are no longer embedded, and `图片附件` is the operator evidence summary. Production `开发0804期` read-back: sheet `认领复核明细`, 10 headers, 123 rows. Backup `/opt/hengzhe-new-product/app/backups/review_export_finish_20260806_182748`; current frontend assets `assets/index-DcXWbuZn.js` and `assets/index-BDwfNXeX.css`; `/api/health` 200.
- 2026-08-06 export center approved-claim correction: previous `认领复核明细` export still only contained confirmed not-claim rows when the period had no stocking requests, because approved claim rows were incorrectly sourced from the stocking/export chain. Fixed `/stocking/traceability-list`, export-period traceability counts, and `/stocking/traceability/export` to use supervisor review rows directly: `claim + approved` and `reject + confirmed_not_claim`; `returned_for_supplement` is excluded. Production `开发0804期` read-back after fix: `253` rows total (`认领=130`, `不认领=123`), 10 expected headers, and `图片附件` populated from claim evidence summaries for 33 rows. Backup `/opt/hengzhe-new-product/app/backups/review_export_claim_rows_20260806_184433`; `/api/health` 200.
- 2026-08-06 export center two export types: user clarified the original central table must remain and the 10-column claim/review export is an additional export type, not a replacement. Added separate `/stocking/traceability/central-export` and frontend button `导出中央表`; existing `/stocking/traceability/export` remains `导出认领情况表`. Both use the same reviewed-row scope (`claim+approved`, `reject+confirmed_not_claim`). Production `开发0804期` read-back: both exports have 253 rows (`认领=130`, `不认领=123`); central export sheet `中央字段导出` starts with central headers, claim export sheet `认领复核明细` has the 10 claim/review headers. Backup `/opt/hengzhe-new-product/app/backups/two_traceability_exports_20260806_185150`; current frontend asset `assets/index-D6a31vBJ.js`; `/api/health` 200.
- 2026-08-06 export attachment column removal: user clarified `图片附件` is not needed in exports. Removed the operator evidence attachment column from both `导出认领情况表` and `导出中央表`; the central table still retains its original `产品图片` column. Production `开发0804期` read-back: both exports remain 253 rows and `图片附件` is absent from headers. Backup `/opt/hengzhe-new-product/app/backups/remove_export_attachment_20260806_185847`; `/api/health` 200.
- 2026-08-06 production `开发0804期` supplement repair: user provided `E:/soft/dingding/补充数据.xlsx` with two rows. Production backup `/opt/hengzhe-new-product/app/backups/supplement_0804_20260806_192153/db.dump` was created before the first write. Fixed `YZH077` 10-pack from `YZH07WT10` to `YZH077WT10` across opportunity, snapshots, stocking request, and export rows. For `HXS012GR`, the final user-confirmed rule is that it was sales-added and must enter the operator's pending-claim queue, not inherit the existing HXS012 approved claim. Follow-up backups: `/opt/hengzhe-new-product/app/backups/hxs012gr_pending_claim_20260806_192734/db.dump` and `/opt/hengzhe-new-product/app/backups/hxs012gr_full_row_20260806_192937/db.dump`. Final state: `HXS012GR` is active under `开发0804期`, assigned to `姚永运` with one pending `sales_claim` task, no claim/review/stocking rows, full source row recorded from `补充数据.xlsx / Sheet1 / row 1`, and product image uploaded to OSS. Image proxy `/api/media/oss-image` returns 200; direct OSS URL returns 403 by private-bucket design. Bad `YZH07WT10` count is 0 in opportunities/stocking/export rows; production `/api/health` 200.
- 2026-08-06 PLM notice reconciliation: user clarified that `E:/soft/dingding/到货通知-8.6.xlsx` is only a business comparison list; do not use its `调研时间`, `最后一次入库时间`, or `首次上架时间` for system judgment. PLM new-arrival judgment must use the freshly downloaded PLM workbook fields only: group `集团八部`, `最后一次入库时间` present, `首次上架时间` present, and their Beijing dates equal. A production read-only/cache-only re-download for 2026-07-27 through 2026-08-06 was written to `/data/plm/recheck_force_20260806_1948` without database writes or DingTalk sends. Result: 7/27..8/6 PLM new-arrival rows by day = `8, 6, 1, 5, 20, 3, 18, 0, 12, 3, 2`, total 78 unique PLM new-arrival keys. The comparison list has 108 group-eight rows / 100 unique keys; 68 keys match PLM new arrivals, 32 comparison-list keys exist in PLM but are restock/non-new, and 10 PLM new-arrival keys are absent from the comparison list. Local reports: `outputs/plm_notice_compare_20260806/plm_notice_compare_20260806.json` and `outputs/plm_notice_compare_20260806/plm_notice_missing_reasons_20260806.json`. Next notification step must first filter these 78 PLM new arrivals by current system state (already secondary-submitted/listed should not notify) before any real DingTalk send.
- 2026-08-07 PLM/Excel secondary dry-run only: user asked to download 2026-08-06 PLM and put all `E:/soft/dingding/到货通知-8.6.xlsx` rows into secondary research, while clearing old secondary tasks. No database write or DingTalk send was performed because the dry-run exposed a business conflict. Production live PLM 2026-08-06 was downloaded to `/data/plm/recheck_force_20260807_0806/plm-2026-08-06.xlsx` without overwriting the normal cache; row_count 2376, first-listing-equals-file-date new arrivals 3: 陆俊全 `CJA1247A`, 潘露英 `JYBAG064-A4`, 郑纯华 `FR038M`. The Excel file has 296 non-empty rows, 108 group-eight rows, 100 unique `country+salesperson+main_sku+sub_sku`. Combined Excel + 8/6 PLM candidates = 103 unique keys. Current system state for those 103: 97 already have submitted secondary research (`historical_secondary_submitted`), 5 can be opened on existing claims, 1 missing opportunity should be created as a secondary entry (`YY03/YY003`, 泰国, 陆俊全). Current production pending secondary tasks = 8, and none are in the Excel/8-6 candidate set; all 8 are old `PLM新增到货` pending tasks. Local reports: `outputs/plm_0806_secondary_20260807/notice_0806_group8_rows.json`, `outputs/plm_0806_secondary_20260807/plm_0806_download_report_20260807.json`, `outputs/plm_0806_secondary_20260807/secondary_0806_dry_run_20260807.json`. Before writing, user must decide whether to reopen the 97 already-submitted secondary records or leave submitted history intact and only create/open the 6 not-submitted candidates while clearing the 8 stale pending tasks.
- 2026-08-07 PLM/Excel secondary production apply: user confirmed preserving the 97 already-submitted secondary records and not duplicating them. Production backup before write: `/opt/hengzhe-new-product/app/backups/secondary_0806_apply_20260807_092954/db.dump`, SHA256 `b470addd0411a5348e1f2c570e87b157050650ac0f122035c6af0f8059c0c967`. Applied result: opened 5 existing claims for secondary research, created 1 missing PLM discovery opportunity/claim (`YY03` / `YY003`, 泰国, 陆俊全, batch `PLM新增到货`), skipped 97 submitted records unchanged, cleared 8 stale pending secondary tasks, created 3 PLM arrival batches, 6 PLM arrival items, and 6 arrival records. Local apply result: `outputs/plm_0806_secondary_20260807/secondary_0806_apply_result_20260807.json`. Production DB verification: the 6 opened/created claims are `waiting_secondary_research` with no submitted timestamp and each has an arrival record; current pending secondary total is 6; the 8 cleared old tasks no longer have `waiting_secondary_research`; 97 submitted records were not reopened. No DingTalk cards were sent in this apply step. Production health after write: `/api/health` ok, API container healthy, memory available about 1.9 GB, swap unused.
- 2026-08-07 secondary submitted-field correction: production read-only check found user examples are data-state issues, not a frontend mapping loss. `ZMZTOY160` has empty secondary fields but listing evidence; `BWHG1775` had only conclusion/link; `HCK021` current pending rows have conclusion/positioning/link/image but no target daily sales or selling points; `WPHG511` is empty. After backup `outputs/db_backups/pre_secondary_reopen_20260807_113942.dump` (SHA256 `eac2f83b829bdb142d08d5c3c12fa316e5b3e822fd916964b08e5afa01bee65a`), 111 `historical_secondary_submitted` rows with incomplete required secondary fields and no listing evidence were reopened to `waiting_secondary_research`; existing non-empty fields were preserved and no target/selling value was fabricated. 469 rows with listing/binding evidence stayed historical submitted. Frontend source-link labels were deployed so source URLs display `链接1/链接2/同链接1` with color chips. Production assets: `assets/index-CFgBlP3P.js`, `assets/index-DuLeu4j3.css`; `/api/health` OK. Note: `docker compose up -d frontend` also recreated dependencies once; future frontend-only deploys should use `--no-deps` or an explicit hot-static-file path to avoid API/Postgres restarts.
- 2026-08-11 商品看板期数修复：生产补齐 `GET /product-board/business-periods` 路由，并将商品看板期数/行数据过滤收窄为隐藏明确脏期与 `history_selection34` 旧选品3/4归档源；`PLM新增到货` 保留展示，因为它是当前到货待二调入口。前端商品看板点击详情增加按 opportunity id 拉取兜底，避免看板分页/轻量列表中缺少完整 opportunity 时点主 SKU 无反应。随后修正前端期数下拉根因：`ProductBoardView` 不再从当前 300 条看板行推断期数，只使用专门的 `productBoardPeriods()` 全量期数接口。生产备份：`/opt/hengzhe-new-product/app/backups/product_board_period_fix_20260811_104915/`、`/opt/hengzhe-new-product/app/backups/product_board_router_before_20260811_105329.py`、`/opt/hengzhe-new-product/app/backups/product_board_dropdown_frontend_before_20260811_110126/`。验证：本地 `python -m pytest backend/tests/test_product_board.py::test_product_board_business_periods_use_active_board_scope -q` 通过；`node --test frontend/tests/productBoard.test.ts frontend/tests/productDetailNavigation.test.ts` 17 条通过；本地 `npm run build` 通过；生产 `api/frontend` 已重建，`/health` OK，读回商品看板期数 29 个，`PLM新增到货` 在期数和行数据中，`小货老品-4月底/历史归档/历史选品3/4全Sheet/5.26期/8.4期` 均不在期数或行数据中；生产前端资源为 `assets/index-DX10EiD5.js`。
- 2026-08-11 14:00 PLM 到货待分配全量部署：用户确认可以按此前方式全量部署生产。生产目标为 `hz-new-product-preprod:/opt/hengzhe-new-product/app`（8080 栈），不是 `hermes-remote` 上的 18081 开发栈。部署包使用本地已提交 `d8444e7` 的 `git archive`，不包含未跟踪文件；保留生产 `.env`、数据库卷、OSS/上传卷、日志和运行脚本。锁：`/opt/hengzhe-new-product/.codex_locks/new_product_workflow_prod.lock`。备份：源码 `/opt/hengzhe-new-product/backups/app_before_full_d8444e7_20260811_135828.tgz`；数据库 `/opt/hengzhe-new-product/app/backups/prod_before_full_d8444e7_20260811_135828.dump`（已用 `pg_restore -l` 校验）。部署后 `RELEASE_COMMIT=d8444e7`，`api/frontend/worker/scheduler/reverse-proxy` 已重建，`postgres/redis` 保持原卷运行。验证：生产 `/api/health` OK，首页 HTTP 200，`migrate` 容器 `Exited (0)`，关键计数 `new_product_opportunity=5478`、`sales_claim_forecast=5435`、`plm_arrival_item=19571`、`arrival_record=655`，日志尾部未见 error/exception/traceback/oom/502。生产代码读回包含 `assignment_block_reason` / `assignment_hint` 字段和前端阻断提示。
- 2026-08-11 17:29 PLM 站点归属规则收口已部署：本地提交 `15321df fix: route PLM arrivals by site owner` 全量部署到 `hz-new-product-preprod:/opt/hengzhe-new-product/app`。新规则：PLM 新品只有在 `PLM销售员` 是当前启用运营且负责该国家/站点时，才自动创建/复用商品与责任并进入该运营待二调；否则停在 `pending_assignment`，由主管/超管在“到货待分配”里选择当前承接运营。`徐仔云`、`徐成芬`、`罗艳娇`、`闫歌` 已调整为启用主管、停用运营、关闭通知；不再出现在运营承接下拉，也不会收到真实运营卡片。生产备份：`/opt/hengzhe-new-product/app/backups/deploy_plm_site_owner_20260811_172906/`、`/opt/hengzhe-new-product/app/backups/admin_role_cleanup_20260811_173136/db.dump`。
- 2026-08-11 17:39 PLM 待分配补漏收口：8 月 3 日至 8 月 10 日 `pending_assignment` 中按新站点规则可自动承接的 5 条已打开二调并发送补充到货卡：陆俊全 `BWTOY0089`、`ZMZTOY193`，庞莹莹 `CASP268`、`DYTOY5014`。剩余 `pending_assignment=32`，且 `auto_fixable_pending=0`，这些不能自动分给 PLM 原销售员，必须等待主管/超管指派。补充通知日志键 `dingtalk_card:arrival_repair:2026-08-07:庞莹莹`、`dingtalk_card:arrival_repair:2026-08-07:陆俊全`、`dingtalk_card:arrival_repair:2026-08-08:陆俊全` 均读回 `sent`。待分配列表读取性能已从约 45 秒降到 59 行约 0.78 秒。
- 2026-08-11 18:02 PLM 待分配改为主 SKU 整组指派并已上生产：部署提交 `7416399 fix: assign PLM arrivals by main SKU group` 到 `hz-new-product-preprod:/opt/hengzhe-new-product/app`。主管/超管在二次调研页“到货待分配”按 `国家/站点 + 主SKU` 看到一组 PLM 到货；选择承接运营后整组子 SKU 一次性进入该运营待二调，关闭也是整组关闭。候选运营来自分配台人员配置：必须是启用运营、通知启用且负责该站点；主管/管理员/测试账号不进入候选，也不会收到运营通知。若同组任一子 SKU 命中多个当前可见商品，整组阻断并要求先处理商品归属，避免拆散同主 SKU。生产备份 `/opt/hengzhe-new-product/app/backups/deploy_plm_group_20260811_180123/`，部署日志 `/opt/hengzhe-new-product/app/logs/deploy_plm_group_20260811_180123.log`，锁 `/opt/hengzhe-new-product/.codex_locks/new_product_workflow_prod.lock`。验证：本地相关后端回归 80 条通过，生产 `RELEASE_COMMIT=7416399`、`/api/health` OK，接口文件读回包含 `assign-group` / `close-group`。
- 2026-08-11 生产定时状态：crontab 仍为每天 10:00 执行 `/opt/hengzhe-new-product/app/run_plm_daily_notify.sh`，脚本运行 `python -m app.plm_daily_notify_job --date yesterday --send`。因此 2026-08-12 10:00 会按新规则处理 2026-08-11 首次上架新品并发送真实通知；`.env` 中 `PLM_SYNC_ENABLED=false` 不影响该脚本路径，不能据此判断每日 PLM 通知未启用。

## Verification Commands

Use a repository-local temporary directory on Windows:

```powershell
New-Item -ItemType Directory -Force .codex_tmp\pytest | Out-Null
python -m pytest -q --basetemp .codex_tmp\pytest `
  backend/tests/test_historical_selection1_claim_backfill.py `
  backend/tests/test_historical_selection1_import.py `
  backend/tests/test_historical_selection1_state_restore.py `
  backend/tests/test_plm_processing.py `
  backend/tests/test_secondary_research.py `
  backend/tests/test_listing_observations.py
```

Then run affected frontend tests, `npm test`, `npm run build`, and `git diff --check`. Do not skip failures by weakening assertions or masking source-data gaps.

## Document Ownership

- Business decisions: `docs/2026-07-09-已确认需求记录.md`.
- Current execution order and detailed timeline: `docs/20-项目推进总控.md`.
- Downstream/PLM/FineBI rules and unresolved external inputs: `docs/21-后半段需求领导对齐问题清单.md`.
- Implementation and verification ledger: `docs/02-功能实现状态.md`.
- Environment and deployment: `docs/06-部署与服务器准备.md`.
- This file must stay short. Move completed timestamps, old release packages, and verbose run logs to the documents above or the weekly work log instead of appending them here.
### 2026-08-10 FineBI 0730-0805 周数据手动抓取与周四定时脚本

- 用户确认 Item/FineBI 应按“每周四抓上一完整周四至周三”执行；2026-08-06 对应周期为 `2026-07-30` 至 `2026-08-05`，标签 `0730-0805`，不包含 8/6 当天销售日。
- 生产手动执行前备份：`/opt/hengzhe-new-product/app/backups/finebi_0730_0805_20260810_003748/db.dump`，SHA256 `8b9f15fcfd0d5de20e0f9aa6b9d2d5e9fc899a2d58ce0f6a87e4e7809fccb233`。
- 首次使用生产现有 `finebi_auto_pull.pull_and_import` 后发现风险：该路径会用 FineBI 主 SKU 自动创建 `history_finebi` listing，并按 `week_number` 覆盖旧周。本次立即清理误创建的 751 个 FineBI-only listing、751 个对应观察周、914 条绑定。
- 随后从备份临时库恢复 149 条被覆盖的旧观察周，删除 39 条坏新增周，再按安全规则重跑：只匹配已有 `history_finebi` 刊登绑定，不创建 listing；按日期合并最近最多四个有数据周。
- 最终生产读回：`listing_record=660`、`active_listing=660`、`item_observation_period=988`，其中 `0730-0805` 为 188 条，全部 `record_source=history_finebi`；平台当前 `platform` 周期未命中该周期。
- FineBI 原始文件保存在 API 容器 `/data/finebi/0730-0805.xlsx`，大小约 2.0MB，SHA256 `6b27c027e8ca1e9019f8054fc83a373fe703883d5df1cc60a54715f5dff459d5`。
- 新增生产周四定时脚本：`/opt/hengzhe-new-product/app/run_finebi_weekly_existing_listings.sh`；crontab 为 `10 8 * * 4 ...`。脚本只更新已有刊登绑定和已有平台观察周期，不再从 FineBI 创建新 listing。dry-run 验证 `0730-0805`：`finebi_items_total=16421`、`history_matched=188`、`history_created=0`、`platform_updated=0`。
- 2026-08-10 02:17 Asia/Shanghai 生产已禁用旧超管 FineBI 手工入口：前端超管后台不再展示 `FineBI 拉取` 分区；后端 `POST /api/admin/finebi/pull` 保留路径但固定返回 403 `FineBI/Item 周数据已改为每周四自动拉取；禁止手工触发。`。后续 FineBI/Item 更新只走周四 08:10 自动任务。
- 后续注意：不要直接开启旧 `scheduler.py -> finebi_auto_pull.pull_and_import` 路径，除非先修成“只更新已有 listing + 日期维度最近四周”，否则会再次制造 FineBI-only listing 和旧周覆盖。
