# 2026-07-14 生产切换实施计划

## 成功标准

- 本地开发代码与 `hengzhe-code-bf4d67d.zip` 基线一致，并保留本机环境、上传、数据库、依赖和业务资料。
- 导入页不再显示历史硬编码期数；选品1、选品2的最近导入结果互不覆盖。
- 已确认的商品详情导航、图片预览、长链接和侧栏压缩在最新基线上可用。
- 财根导入、开放认领、认领提交、主管复核的测试通过，角色管理不存在经理提权入口。
- 前端测试与构建、后端完整测试、Compose 配置检查全部通过。
- `101.132.26.138` 旧库和完整备份均保留，新生产库只包含账号和人员配置，业务表为空。
- 正式生产启用钉钉通知和必要调度；`139.224.2.166` 使用独立开发库并关闭真实自动推送。

## 任务 1：隔离工作区并恢复线上基线

**文件范围：** `.gitignore`、独立 Git worktree、`hengzhe-code-bf4d67d.zip` 解压源码。

1. 保留当前 `main` 上未提交的旧版 UI 修改，不覆盖或清理。
2. 将 `.worktrees/` 加入忽略列表，在 `lxc/production-cutover` 分支创建独立 worktree。
3. 将压缩包中的源码覆盖到独立 worktree，排除 `.env*`、数据库、上传、依赖、虚拟环境和本地资料。
4. 对比压缩包与 worktree 的受控源码目录，确认恢复完整。

**验证：** `git status --short`、压缩包源码目录逐项比较、敏感文件未进入 diff。

## 任务 2：建立最新基线测试

1. 安装或复用现有前端、后端依赖。
2. 先运行前端现有测试与构建。
3. 运行后端完整 `pytest`，记录基线失败，不把既有失败误归因于新修改。
4. 运行 `docker compose config`，验证部署声明可解析。

**验证：** 测试命令退出码及失败详情留痕。

## 任务 3：修复导入页面状态

**预计文件：** `frontend/src/App.tsx`、相关前端测试文件。

1. 先添加失败测试：初始期数为空；选品1和选品2分别更新最近导入结果。
2. 删除 `开发0623期`、`5.26期` 等硬编码初始值。
3. 保留选文件后读取 Sheet 的行为；导入成功后只更新对应卡片的 `source_sheet` 和 `business_period`。
4. 导入批次列表继续刷新，失败导入不更新成功状态。

**验证：** 新测试先失败后通过，前端测试与构建通过。

## 任务 4：核对并修复商品详情 UI

**预计文件：** 最新基线中的商品看板组件和 `frontend/src/styles.css`。

1. 核对上一个/下一个是否按“同主 SKU 子 SKU -> 相邻主 SKU”顺序切换。
2. 核对当前子 SKU 高亮且详情字段随之切换。
3. 将图片预览固定到最高全屏层级并允许内部滚动，避免被表格或固定按钮遮挡。
4. 将长 URL 显示为紧凑域名/打开链接控件，保留完整链接目标和可访问标题。
5. 压缩主管/运营统计侧栏，不改变统计含义。

**验证：** 逻辑测试、前端构建、桌面与窄屏浏览器截图检查。

## 任务 5：修复生产代码风险

**预计文件：** 管理路由及测试、Compose 卷配置。

1. 先添加失败权限测试：经理不能新增或修改角色映射，超级管理员可以。
2. 将角色映射写操作限制为 `super_admin`，不放宽其他接口权限。
3. 为源表上传和证据图片目录添加命名卷，防止 API 容器重建丢失本地文件。
4. 检查健康接口、API 文档暴露、登录/密码和通知配置；只做有测试或配置验证支持的最小修复。

**验证：** 权限测试先失败后通过，完整 pytest 与 `docker compose config` 通过。

## 任务 6：验证财根数据流和导出前流程

1. 检查选品2字段映射、主销售员分配、无主销售员开放认领及源表追溯字段。
2. 验证开放机会可由运营认领，重复/并发操作不会越权或制造无效状态。
3. 验证认领提交后进入主管复核，批准、驳回、不认领和补充资料分支状态正确。
4. 验证到导出前的入池、分配、认领、复核、可导出统计一致。

**验证：** 相关定向测试和完整后端测试；输出流程风险清单。

## 任务 7：切换 `101.132.26.138` 正式生产库

1. 记录当前容器、数据库、迁移版本、配置开关和表计数。
2. 暂停外部写入及 worker/scheduler。
3. 对当前库执行带时间戳的 `pg_dump -Fc`，检查非空并运行 `pg_restore --list`。
4. 创建 `workflow_prod_20260715`，执行最新 Alembic 迁移。
5. 按外键顺序复制 `users`、`user_password`、`role_mapping`、`operator_assignment_profile`，核对行数。
6. 备份服务器 `.env`，只切换数据库名及经核验的生产开关；旧库不改不删。
7. 部署已验证代码，启动 Compose，开启钉钉自动通知、用户同步和必要调度，清除测试接收人覆盖。
8. 建立每日数据库备份并保留有限周期，验证备份命令实际成功。
9. 验证健康检查、登录权限、配置表行数、业务表为零和通知依赖。

**回退：** 恢复旧 `.env` 数据库名和旧代码包后重启 Compose；旧库及备份始终保留。

## 任务 8：部署 `139.224.2.166` 开发环境

1. 只读检查系统、CPU、内存、磁盘、Docker/Compose 和端口。
2. 通过一次性认证安装本机公钥，不在文件、日志或命令历史中保存密码。
3. 部署同一已验证代码，创建独立开发数据库、Redis 数据和随机密钥。
4. 默认关闭真实钉钉自动推送和生产同步，不迁移生产业务数据。
5. 验证迁移版本、容器健康、API、前端和最小登录能力。

## 任务 9：最终验证与交付记录

1. 复查 diff，确认无密钥、`.env`、数据库或业务上传进入 Git。
2. 重新运行前端测试/构建、后端完整 pytest、Compose 配置检查。
3. 汇总两台服务器地址、数据库名称、备份路径、通知状态、部署版本、剩余风险和回退步骤。
4. 追加工作日志；提交代码时只包含本次明确修改。

