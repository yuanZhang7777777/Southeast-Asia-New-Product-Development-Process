# 部署与服务器准备

> Updated: 2026-07-22 Asia/Shanghai
> 用途：区分生产与开发环境，并记录开发环境部署、备份和验证方法。

## 1. 环境边界

| 环境 | 服务器 | 外部端口 | 用途 | 操作规则 |
|---|---|---:|---|---|
| 生产 | `101.132.26.138` | `8080` | 当前业务人员使用 | 本项目后续开发不得连接、部署或重启；任何生产变更必须另行审批并安排非工作时间窗口 |
| 开发 | `139.224.2.166` | `18081` | 后续开发和业务验证 | 只在此环境执行新迁移、重建开发容器和测试新功能 |

两个环境的数据库、Redis、上传文件、容器卷、端口和环境变量必须完全隔离。不得从开发环境连接生产数据库，也不得把 `.env`、密码、Token、私钥或 Cookie 提交到 Git。

## 2. SSH

本机使用 SSH 别名：

```powershell
ssh hz-new-product-dev
```

连接配置保存在本机 `~/.ssh/config`，仓库只保留不含密码的 `deploy/ssh-config.example`。不要在命令、文档或脚本中再次硬编码服务器密码。

生产别名即使仍存在，也不得用于日常开发、检查或部署。

## 3. 开发服务器基线

2026-07-16 只读复核结果：

| 项 | 开发服务器 |
|---|---|
| 系统 | Linux 4.18 x86_64 |
| 内存 | 约 15 GiB |
| 系统盘 | 500 GiB，已用约 8% |
| Docker | 26.1.3 |
| Docker Compose | v2.27.0 |
| Compose 项目名 | `hengzhe-new-product-dev` |
| PostgreSQL 数据库 | `workflow_dev_20260715` |
| 应用目录 | `/opt/hengzhe-new-product-dev/app` |
| 备份目录 | `/opt/hengzhe-new-product-dev/backups` |
| 发布包目录 | `/opt/hengzhe-new-product-dev/releases` |

开发栈包含 `postgres / redis / migrate / api / worker / scheduler / frontend / reverse-proxy`，对外只映射 `18081 -> reverse-proxy:80`。PostgreSQL 和 Redis 不映射公网端口。

### 3.1 开发部署命令门禁

所有开发服务器 Compose 命令都必须在 `/opt/hengzhe-new-product-dev/app` 下显式指定项目名和环境文件：

```bash
cd /opt/hengzhe-new-product-dev/app
test "$PWD" = "/opt/hengzhe-new-product-dev/app"
docker compose -p hengzhe-new-product-dev --env-file .env config --quiet
```

后续命令必须沿用 `docker compose -p hengzhe-new-product-dev --env-file .env ...`；不得裸跑 `docker compose`，不得启用 `notification` profile。`deploy/server-bootstrap.sh` 硬编码生产路径，`hz-new-product-preprod` 指向生产服务器，两者均不得用于开发部署。

开发库不是生产库副本。当前已配置本地 `Dev Admin` 和刘学城两个开发账号；2026-07-21 刘学城已同时绑定 `super_admin + operator`，并启用泰国站点运营档案，可用同一账号切换主管 / 运营视角完成第一轮单人 UAT。写入前数据库备份为 `/opt/hengzhe-new-product-dev/backups/workflow_dev_before_liu_uat_20260721_101855.sql.gz`。其余生产业务账号、运营档案和业务数据均未复制；未初始化的生产姓名在开发环境登录会返回“用户未配置”。需要更多业务人员验证时，应单独初始化开发账号和开发数据，不得直接连接或复用生产数据库。

## 4. 开发环境访问与健康检查

公网入口：

```text
http://139.224.2.166:18081
```

服务器内检查：

```bash
curl -fsS http://127.0.0.1:18081/api/health
docker compose -p hengzhe-new-product-dev --env-file .env ps
```

本机检查：

```powershell
curl.exe -fsS http://139.224.2.166:18081/api/health
```

健康响应必须包含 `status=ok`、`environment=development`。前端首页可访问不等于 API、迁移和数据库都正常，仍需同时检查 API 健康和容器状态。

### 4.1 ERP 体积查询配置

销售自选 / 备货申请会读取以下五项服务器环境变量；只允许写入各环境独立的 `.env`，不得写入 Git、部署包、日志或文档：

```text
ERP_LOGIN_URL
ERP_PRODUCT_LIST_URL
ERP_DOWNLOAD_LIST_URL
ERP_USERNAME
ERP_PASSWORD
```

五项中任一缺失、查询失败或尺寸不完整时，接口必须返回“需要手填”而不是泄露配置或阻断整个申请。当前开发部署允许填写长、宽、高（cm），由前后端计算体积并记录来源 `manual`。2026-07-22 只读预检确认开发环境仍未提供这五项，引用的资产文档也没有可唯一定位的精确凭据；不得猜测或写入 `.env`，真实 ERP 查询待实际凭据安全配置后再验收。

ERP 登录使用现行 Open Login 获取临时令牌，业务请求使用 `Authorization: <accessToken>`，不加 `Bearer`，不使用 Cookie 或历史固定 token。包装后三维任一无效时整组回退到产品三维，不混用两组尺寸；两组都不完整时只返回手填降级。

## 5. 当前生产状态

生产工作目录和版本归档：

```text
/opt/hengzhe-new-product/app
/opt/hengzhe-new-product/releases/app-20260716-b373d65
```

2026-07-16 当前流量版本：

```text
API: hengzhe-new-product-api-vb373d65
Frontend: hengzhe-new-product-frontend-vb373d65
Database: workflow_prod_20260715
```

当前前端静态资源：

```text
/assets/index-CRnio9qk.js
/assets/index-E02L5hvi.css
```

`b373d65` 为 API + 前端无中断发布：自动分配改为同站点内负载优先的贪心列表调度，重点品类、人员优先级和自定义顺序仅处理同负载并列；运营负载改为无内部横向滚动的自适应网格，“运营配置”固定紧邻“提交分配”。本次无数据库迁移，未重算既有分配；PostgreSQL、Redis、worker 和 scheduler 未重启且 `RestartCount=0`，旧 API/前端容器保留用于快速回滚。

发布验证：候选 API 健康检查通过；容器内 7 组/3 人公平性模拟为 `3/2/2`；Caddy Admin API 回读上游均为 `vb373d65`；服务器内外 `/api/health` 为 `ok/production`，首页和新 JS/CSS 资源返回 `200`。

发布采用候选容器健康检查后原子重载 Caddy 的方式。发布时不重启 PostgreSQL、Redis、worker 或 scheduler，旧版本容器保留用于快速回滚；`deploy/Caddyfile` 必须同步持久化到当前容器名，避免服务器重启后回退。

注意：`deploy/Caddyfile` 是只读 bind mount。宿主机使用 `sed -i`、覆盖复制等会替换 inode 的操作后，运行中的 Caddy 容器可能仍看到旧文件。无重启切流时应把宿主机持久配置通过标准输入加载，并从 Caddy Admin API 回读实际 upstream；不能只检查宿主机文件：

```bash
docker exec -i hengzhe-new-product-reverse-proxy-1 \
  caddy validate --config - --adapter caddyfile < deploy/Caddyfile
docker exec -i hengzhe-new-product-reverse-proxy-1 \
  caddy reload --config - --adapter caddyfile < deploy/Caddyfile
docker exec hengzhe-new-product-reverse-proxy-1 \
  wget -qO- http://127.0.0.1:2019/config/
```

## 6. 开发部署流程

发布包、备份和回滚目录位于 `/opt/hengzhe-new-product-dev`；所有 `docker compose` 命令必须在 `/opt/hengzhe-new-product-dev/app` 下执行：

1. 确认目标是 `hz-new-product-dev`，不要连接生产服务器。
2. 对开发 PostgreSQL 执行带时间戳的 `pg_dump`，写入 `backups/`。
3. 上传不含 `.env`、测试数据库、缓存、`node_modules` 和私密源表的发布包。
4. 保留服务器现有 `.env`；发布包不得覆盖它。
5. 运行 `docker compose config`，确认项目名和 `18081` 映射。
6. 执行 `docker compose build` 和 `docker compose up -d`；`migrate` 只升级开发库。
7. 检查迁移版本、容器健康、API 健康、首页和关键接口。
8. 写入 `DEPLOYED_COMMIT`，并保留发布包和数据库备份用于回滚。

开发运行库已在备份后从 `a8d4e6f7b901` 沿单一迁移链 `c9d1e2f3a456 -> d0e2f3a4b567 -> e1f2a3b4c678` 升级并回读到 `e1f2a3b4c678`：`c9d1e2f3a456` 加入正式备货申请关联和 `stocking_request.unit_volume_source`，`d0e2f3a4b567` 加入 `export_row.selection_source`，`e1f2a3b4c678` 加入 `stocking_request.length_cm / width_cm / height_cm`。以上新迁移均未应用到生产库。

## 7. 回滚

应用代码回滚优先使用 `releases/` 中上一份发布包，并保留当前 `.env` 和 Docker volumes。数据库结构回滚前必须再次备份；若新表已经出现真实业务数据，不得直接执行 Alembic downgrade，应先评估并导出数据。

## 8. 安全与备份

- `.env` 永不入库，发布包不得包含 `.env`。
- 开发环境每次迁移前先备份 PostgreSQL。
- 上传文件和数据库卷不随代码目录替换。
- 外网只开放必要的 SSH 与 `18081`；数据库、Redis、worker、scheduler 不直接暴露。
- 日志不得记录含 Token 的完整访问 URL。
- 生产服务器在用户使用期间绝不重启。
- 2026-07-17 已在生产和开发各自的 `.env` 保存 PLM 接口配置与业务账号凭据；两边 `PLM_SYNC_ENABLED=false`，本次未重启或重建服务。实际凭据不写入仓库、文档或日志。
- 独立服务 `caigen-arrival-notifier` 与新品流程生产环境共同运行在 `101.132.26.138`，但新品流程生产环境本身不发送到货卡；到货卡由 `caigen-arrival-notifier` 单独发送。新品流程开发环境运行在 `139.224.2.166:18081`。开发期不配置该通知服务向开发库常驻写入，开发平台只做手动 PLM 验收且不自动发卡；正式平台上线后，才在批准的维护窗口切换数据同步目标和卡片按钮地址。

## 8.1 当前开发发布边界

- `b0f27fde5109` 已在开发环境发布：销售自选三分支、运营备货申请保存 / 提交、主管勾选已提交申请导出、长宽高自动换算、离开编辑区自动保存、固定排序、主管复核定位稳定和紧凑刊登观察工作台均已生效。
- 当前开发运行库与本地源码迁移 head 均为 `e1f2a3b4c678`。
- 用户已确认手填长宽高、自动保存、销售自选三分支、提交和主管勾选导出的写入式业务 UAT 无问题；真实 ERP 查询仍待五项实际凭据安全写入开发 `.env` 后验收。
- 后续开发发布仍必须先生成不含 `.env` 的 `git archive`、备份开发数据库和 `.env`，再回读迁移、提交标记、内外健康、容器身份、安全开关与近期错误日志。
- 生产 `101.132.26.138:8080` 不在本次操作范围内。

## 9. 最近开发部署
### 9.0 当前部署：`b0f27fde5109`

2026-07-22 将刊登与观察紧凑化修复仅部署到独立开发环境：

- 范围：压紧筛选、批量栏、主 SKU / Item / 周记录间距；复选框取消全局输入宽高；单结果顶部对齐；批量栏在 561–900 px 保持单行，仅窄手机纵排。无后端、迁移、Compose 或 Caddy 变更。
- 发布包：`/opt/hengzhe-new-product-dev/releases/b0f27fde5109.tar.gz`，SHA-256 `db6b6e2f6cf3251ded69855c9a595db80105d405a86b90b46f4b3d29cd764224`；共 236 项，不含 `.env`、缓存、未跟踪文件或业务源表。
- 发布前开发库备份：`/opt/hengzhe-new-product-dev/backups/workflow_dev_before_b0f27fde5109_20260722_153034.sql.gz`（410909 bytes、权限 600、gzip 校验通过）；环境备份：`/opt/hengzhe-new-product-dev/backups/config/.env_before_b0f27fde5109_20260722_153034`（1415 bytes、权限 600、逐字节回读一致）。
- 回滚目录：`/opt/hengzhe-new-product-dev/app.previous_b0f27fde5109_20260722_153034`；旧前端镜像：`hengzhe-new-product-dev-frontend-backup:b0f27fde5109_20260722_153034`。
- 本地全量后端 `308 passed`、前端 `129 passed`、TypeScript/Vite 生产构建和 `git diff --check` 通过；服务器提交标记为 `b0f27fde5109`，Alembic 回读为 `e1f2a3b4c678 (head)`。
- 仅 frontend 重建并重建容器；API、worker、scheduler、reverse-proxy、PostgreSQL 和 Redis 容器 ID 均保持不变，`RestartCount=0`。
- `APP_ENV=development`、`HTTP_PORT=18081`、`PLM_SYNC_ENABLED=false`、`WORKFLOW_AUTOMATION_ENABLED=false`、`DINGTALK_CARD_AUTOSEND_ENABLED=false` 保持不变。
- 内外健康返回 `ok/development`；当前资产为 `/assets/index-Dyt4ZkdJ.js`、`/assets/index-BiB-4QeZ.css`。799 px 浏览器实测批量栏 51 px、复选框 13×13 px、收起组 46 px，展开后页面无横向溢出，控制台错误为 0。
- 生产 `101.132.26.138:8080` 未连接、部署、重启或修改。

### 9.1 上一次部署：`36a9ae9`

2026-07-22 将销售自选、正式备货申请和精确导出门槛部署到独立开发环境：

- 发布包：`/opt/hengzhe-new-product-dev/releases/36a9ae9.tar.gz`，SHA-256 `496f493903a5a331fefbeae7e709512b5559768e30f94f98da7fa6390dcf2154`；由 `git archive` 生成，不含 `.env`、未跟踪文件、缓存或业务源表。
- 迁移前开发库备份：`/opt/hengzhe-new-product-dev/backups/workflow_dev_before_36a9ae9_20260722_094053.sql.gz`（权限 `600`）；环境备份：`/opt/hengzhe-new-product-dev/backups/config/.env_before_36a9ae9_20260722_094053`；回滚目录：`/opt/hengzhe-new-product-dev/app.previous_20260722_094053`。
- 开发库从 `a8d4e6f7b901` 经 `c9d1e2f3a456` 升级到 `d0e2f3a4b567 (head)`；服务器提交标记统一为 `36a9ae9`。
- 本地后端全量 305 tests、前端 16 文件 122 tests、TypeScript/Vite 构建通过；开发服务器隔离一次性容器以临时 SQLite 且禁用外部集成运行后端全量，`305 passed in 45.47s`。远端镜像构建、Compose 配置、迁移、图片导入、内外健康和近期日志检查通过。
- 管理员 / 运营只读浏览器 UAT 验证备货申请、销售自选表单、主管勾选导出中心和既有刊登历史，浏览器控制台错误为 0；保存、提交、导出和三分支的写入式业务 UAT 尚未签字完成。
- API、worker、scheduler、frontend 和 reverse-proxy 已重建；PostgreSQL、Redis 未重建，容器 ID保持不变且 `RestartCount=0`，公网端口仍为 `18081`。
- `APP_ENV=development`、`PLM_SYNC_ENABLED=false`、`DINGTALK_CARD_AUTOSEND_ENABLED=false` 保持不变。生产 `b373d65` 未连接、部署或重启。

### 9.2 上一次部署：`c017c5d`

2026-07-21 将商品全局档案和刊登观察工作台 UI 重构部署到开发环境：

- 完整功能包：`/opt/hengzhe-new-product-dev/releases/dd8edd0.tar.gz`，SHA-256 `0aa6638a2197a04619ff5bce94a202834d132c3baba9c43da2c93d725f5fab2a`；未来周期显隐修复包：`/opt/hengzhe-new-product-dev/releases/c017c5d.tar.gz`，SHA-256 `cc4843082c8e088ee022920df7007aa9bec7af7f14ed8c1644ea19569d83ec1b`。两份均由 `git archive` 生成，不含 `.env`、未跟踪文件、缓存或业务源表。
- 发布前开发库备份：`/opt/hengzhe-new-product-dev/backups/workflow_dev_before_dd8edd0_20260721_134420.sql.gz`；环境变量备份：`/opt/hengzhe-new-product-dev/backups/config/.env.before_dd8edd0_20260721_134420`。旧 API/前端镜像标签为 `hengzhe-new-product-dev-api-backup:20260721_134420`、`hengzhe-new-product-dev-frontend-backup:20260721_134420`，修复前前端镜像标签为 `hengzhe-new-product-dev-frontend-backup:20260721_135008`。
- `dd8edd0` 重建 API 和 frontend；`c017c5d` 只重建 frontend。worker、scheduler、PostgreSQL、Redis 和 reverse-proxy 容器 ID 未改变，所有现役服务 `RestartCount=0`。
- Alembic 仍为 `a8d4e6f7b901 (head)`；服务器内和公网 `GET /api/health` 均返回 200、`status=ok`、`environment=development`，服务器版本标记统一为 `c017c5d`，近期 API/前端日志无 Traceback、Exception、Critical 或 Error。
- 本地验证为后端全量 252 tests、二次调研与刊登观察聚焦 10 tests、前端全量 104 tests、TypeScript/Vite 生产构建和 `git diff --check` 通过。
- 真实浏览器确认工作台默认筛选待刊登、选择全部可见完整数据范围、主 SKU 整组收起、结果区独立纵向滚动且无横向溢出；未开始的未来周期即使开发测试数据已提前填入也不会显示。商品详情已分别提供二次调研和刊登观察只读历史。
- `APP_ENV=development`、`PLM_SYNC_ENABLED=false`、`DINGTALK_CARD_AUTOSEND_ENABLED=false` 保持不变。本次未连接、部署或重启新品流程生产环境 `101.132.26.138:8080`。

### 9.3 上一次部署：`24f601f`

2026-07-21 将运营认领 UAT 易用性修复部署到开发环境：

- 最终发布包：`/opt/hengzhe-new-product-dev/releases/24f601f.tar.gz`，SHA-256 `74d28c150fc3d65273144f05d075809fbfb4c22d0d33b282127c5848458081fe`，不含 `.env` 或未跟踪文件。
- 环境变量备份：`/opt/hengzhe-new-product-dev/backups/config/.env.before_24f601f_20260721_113956`；回滚目录：`/opt/hengzhe-new-product-dev/app.previous_20260721_113956`；旧前端镜像标签：`hengzhe-new-product-dev-frontend-backup:20260721_113956`。
- `f26eae8` 完成导出中心移除重复“查看明细”、商品详情矩阵移除逐行提交状态、组导航移动到表格下方、不认领原因改为定制弹层；真实浏览器发现默认 Esc 自动关闭不稳定后，由 `24f601f` 增加显式 Esc 丢弃处理。
- 本次两次增量均只构建并重建 frontend；API、worker、scheduler、PostgreSQL、Redis 和 reverse-proxy 容器 ID 保持不变。服务器内和公网 `GET /api/health` 均返回 200、`status=ok`、`environment=development`，新 JS/CSS 资源返回 200。
- 前端 99 个测试、TypeScript/Vite 构建和 `git diff --check` 通过；真实浏览器确认期数本身可切换导出明细、矩阵布局不再被状态和原因选择器撑开、上下组位于矩阵下方、定制原因弹层外观正常且 Esc 后关闭。
- 上一次前端增量部署后服务器 `CURRENT_COMMIT` 仍停留在旧完整发布 `b564815`；本次已把根目录 `CURRENT_COMMIT` 和应用目录 `DEPLOYED_COMMIT` 统一写为 `24f601f`。
- 为继续二次调研 UAT，先备份开发库到 `/opt/hengzhe-new-product-dev/backups/workflow_dev_before_secondary_uat_20260721_114310.sql.gz`，再通过现有 `/arrival/records` 接口为 `GZMO075` 的 3 个子 SKU 创建开发 UAT 模拟到货记录；未运行真实 PLM，未发送钉钉。
- `APP_ENV=development`、`PLM_SYNC_ENABLED=false`、`DINGTALK_CARD_AUTOSEND_ENABLED=false` 保持不变。本次未连接、部署或重启新品流程生产环境 `101.132.26.138:8080`。

### 9.4 上一次前端增量部署：`48b3639`

2026-07-17 将统一分支提交 `48b3639` 前端增量部署到开发环境：

- 发布包：`/opt/hengzhe-new-product-dev/releases/48b3639.tar.gz`，SHA-256 `087374fc71efea9c172ac40599835086c417f6ee92f18f4627dcd5046de86fd1`，不含 `.env` 或未跟踪文件。
- 环境变量备份：`/opt/hengzhe-new-product-dev/backups/config/.env.before_48b3639_20260717_190940`；回滚目录：`/opt/hengzhe-new-product-dev/app.previous_20260717_190940`；旧前端镜像标签：`hengzhe-new-product-dev-frontend-backup:20260717_190940`。
- 相对上一开发基线没有后端、迁移或 Compose 变更，因此本次只构建并重建 frontend；API、worker、scheduler、PostgreSQL、Redis 和 reverse-proxy 容器 ID 均未改变，开发数据和上传文件未迁移或清空。
- 服务器内和公网 `GET /api/health` 均返回 200、`status=ok`、`environment=development`；首页以及 `index-BmgzCzw2.js`、`index-BLoNZbay.css` 返回 200，近期 frontend/API/reverse-proxy 日志无启动错误。
- API、worker、scheduler 继续加载服务器当前卡片模板配置；`PLM_SYNC_ENABLED=false`、`DINGTALK_CARD_AUTOSEND_ENABLED=false` 保持不变。模板值不写入仓库或文档。
- 本次未连接、部署或重启新品流程生产环境 `101.132.26.138:8080`。

### 9.5 上一次完整开发部署：`b564815`

2026-07-17 将前后半段统一分支提交 `b564815` 部署到开发环境：

- 发布包：`/opt/hengzhe-new-product-dev/releases/b564815.tar.gz`，SHA-256 `25e0096e232a6c40fe6091fdd6e73f196b978cb43d04703272bc8485d958e42d`，不含 `.env` 或未跟踪文件。
- 部署前数据库备份：`/opt/hengzhe-new-product-dev/backups/workflow_dev_before_b564815_20260717_173500.sql.gz`。
- 环境变量备份：`/opt/hengzhe-new-product-dev/backups/config/.env.before_b564815_20260717_173500`，权限 `600`。
- 回滚目录：`/opt/hengzhe-new-product-dev/app.previous_20260717_173500`。
- API、worker、scheduler、frontend 和 reverse-proxy 已更新；PostgreSQL、Redis 容器 ID 未改变，启动时间仍为 2026-07-15，`RestartCount=0`。
- Alembic 为 `a8d4e6f7b901 (head)`；服务器内和公网 `GET /api/health` 均返回 `status=ok`、`environment=development`，首页、`index-2hqdAZ-1.js` 和 `index-6y7olTHB.css` 返回 200。
- 公网前端包同时包含导出中心、二次调研、图片粘贴上传和刊登与观察入口；近期 API、worker、scheduler 日志无 Traceback、Exception、Error 或 Critical。
- 开发 `.env` 已增加非秘密的当前卡片模板配置，但 `PLM_SYNC_ENABLED=false`、`DINGTALK_CARD_AUTOSEND_ENABLED=false` 保持不变；模板值不写入仓库或文档。
- 2026-07-17 新建并验证绑定 `action_url` 的卡片模板，刘学城点击可打开开发商品看板；开发 `.env` 已持久切换，API、worker、scheduler 重建并确认加载新模板，健康检查正常。配置备份为 `/opt/hengzhe-new-product-dev/backups/config/.env.before_d339_20260717_181206`；未重建 PostgreSQL、Redis、frontend 或 reverse-proxy。
- 只读核对了同机独立服务 `caigen-arrival-notifier` 的模板配置；未部署、重启或修改新品流程生产环境及该通知服务。
