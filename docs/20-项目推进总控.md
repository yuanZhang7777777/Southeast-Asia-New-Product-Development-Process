# 东南亚新品流程项目推进总控

> Updated: 2026-07-17 Asia/Shanghai
> 用途：作为项目推进的一页式总控。想知道“做到哪了、下一步做什么、哪些文档看什么”，先看这里。

## 1. 当前阶段

当前不是需求从零确认阶段，也不是只确认导入口径阶段。

当前阶段是：

```text
前段 MVP 稳定运行
  -> 前半段与后段代码已合并为统一开发分支
  -> 统一版本已验证并部署开发环境
  -> 运营 / 主管完成独立开发环境业务验收
  -> 获取周 Item 真实接口并接自动调度
  -> 生产窗口另行审批
```

第一版范围已经冻结：

- 只处理 `选品1：海外仓开发部门开发新品认领-反馈`。
- 只处理 `选品2：海外仓财根团队开发新品认领-反馈`。
- 做导入、主管分配、运营认领/不认领、主管复核、导出。
- 不做在线表自动写回入口。
- 不做选品3、选品4、直发转海外仓、老品来源。
- 不做采购/供应链/下单待办。

## 2. 已完成到哪里

| 模块 | 当前结论 |
|---|---|
| 后端基础 | 已完成 FastAPI、数据库模型、迁移、PostgreSQL 生产保护 |
| 选品1导入 | 已完成，按中央表 BV 前字段/表头快照处理 |
| 选品2/财根导入 | 已完成，按财根 SPU/SKU/进价/多销售员认领结构处理 |
| 主管分配 | 已完成，一键分配按主 SKU 组走 |
| 运营认领 | 已完成，支持认领单销、不认领原因、批量提交完整项 |
| 主管复核 | 已完成，通过认领、确认不认领、退回补充 |
| 商品看板 | 已完成第一版，全量状态展示，和机会池分开 |
| 导出 | 已完成备货申请表和新品中央字段导出 |
| 后段二次调研 | 五种定位和最新分流已完成；调研图片支持选择、拖拽和 Ctrl+V 粘贴，调研结论粘贴图片自动归入当前子 SKU。 |
| 刊登与观察 | 已确认差距全部收口并与生产前半段代码合并；淘汰提醒按进入事件去重、跨业务期承接、详情按来源业务期分组、已复盘再次编辑、停止后完成已取数周期、首次成功指标冻结、浏览器本地草稿，以及并发写入行锁和复用审计均已实现 |
| 统一开发基线 | `lxc/integrated-workflow@1aae41e` 已部署 `http://139.224.2.166:18081`；生产仍为 `b373d65` |
| 测试 | 已部署基线后端 246 个测试、前端 87 个测试及生产构建通过；当前未部署图片粘贴上传改动前端 95 个测试及生产构建通过；Alembic 单一 head 为 `a8d4e6f7b901` |
| 真实字段核对 | 已完成选品1/选品2到备货申请表字段口径核对 |

详细状态看：`docs/02-功能实现状态.md`。

## 3. 还没完成什么

| 优先级 | 未完成项 | 说明 |
|---|---|---|
| P0 | 开发环境端到端业务验收 | 用 demo 数据和少量真实 Excel 跑完整前后段链路 |
| P0 | UI / 流程细节修正 | 只修业务验收暴露的导入、分配、认领、复核、看板、导出、二次调研和刊登观察问题 |
| P1 | 钉钉通知接入 | 接工作通知/链接卡片，让用户点通知进入平台处理 |
| P1 | 钉钉 H5 免登 | 在钉钉内打开平台时识别当前用户和角色 |
| P1 | 开发环境业务验收 | 单表工作台已部署到独立公网 18081 开发入口；待运营/主管用实际账号完成业务操作验收 |
| P1 | 文件存储 | OSS 已验证可上传/删除；部署时需清理旧 OSS 环境变量，并保留服务器本地文件兜底 |
| P2 | 周 Item 真实接口与调度 | 业务表和落库边界已完成；待业务提供实际接口、鉴权和未过滤聚合数据样例 |

## 4. 下一步推进顺序

当前优先顺序为：先由运营 / 主管完成独立开发环境业务验收，再接周 Item 真实接口和自动调度；下列前段演示步骤保留为回归路径，不再代表唯一主线。

### Step 1：开发环境业务验收

目标：让运营和主管确认统一版本符合真实业务操作。

要做：

- 使用 `http://139.224.2.166:18081`，不得在生产环境做写入式验收。
- 用开发账号和少量可控数据检查商品看板、机会池、分配台、运营认领、主管复核、按期导出、二次调研和刊登观察。
- 记录实际操作问题，再按影响程度做局部修正。

### Step 2：用少量真实 Excel 走完整链路

目标：确认真实 `选品1`、`选品2` 能从导入走到导出。

要做：

- 用少量真实 `选品1` 数据导入。
- 用少量真实 `选品2/财根` 数据导入。
- 主管一键分配。
- 运营认领/不认领。
- 主管复核。
- 导出两个 Excel：
  - `海外仓备货申请表` 结构。
  - 新品中央字段追溯导出。

### Step 3：修业务演示中暴露的问题

只修影响演示和试用的问题：

- 页面文案不对。
- 状态看不懂。
- 按钮位置不合理。
- 商品卡片信息缺失。
- 导出字段不对。
- 真实 Excel 导入失败。

不在这个阶段新增后段大功能。

### Step 4：接入钉钉通知和入口

目标：用户能在钉钉里收到通知，点击后进入我们的平台处理。

第一版钉钉做：

- 企业内部应用 / 网页应用作为入口。
- 新品待办互动卡片发送个人任务提醒。
- 卡片按钮带平台入口链接。
- 链接进入具体任务或模块。
- 后端记录通知发送日志和去重 key。
- 工作通知 `link` / `action_card` 保留为兜底。

第一版钉钉不做：

- 钉钉待办。
- 在线表自动写回。
- 聊天机器人复杂交互。
- 真正的卡片内直接填写业务数据。

已确认钉钉卡片接入文档：

| 内容 | 文件 |
|---|---|
| 新品待办卡片模板、变量、createAndDeliver 调用方式 | `docs/19-钉钉新品待办卡片接入说明.md` |
| 最新模板 ID | `a428e864-5416-4ea6-ad29-36e19e0615e2.schema` |
| 当前后端最小 sender | `backend/app/dingtalk_card_sender.py` |

### Step 5：生产发布评估

目标：开发环境 UAT 完成后，再决定是否申请生产发布窗口。

要做：

- 汇总开发环境 UAT 结果和未解决问题。
- 单独申请非工作时间生产窗口。
- 发布前备份生产数据库，构建候选容器并验证健康。
- 发布过程不重启 PostgreSQL、Redis、worker 或 scheduler；切流后回读实际上游和静态资源。

## 5. 文档分工

| 文档 | 作用 | 谁先看 |
|---|---|---|
| `docs/20-项目推进总控.md` | 项目做到哪、下一步做什么 | 项目推进时先看 |
| `AGENT_HANDOFF.md` | 新会话交接、最新上下文、禁区 | 新开会话先看 |
| `docs/02-功能实现状态.md` | 已实现、已验证、未完成状态台账 | 判断完成度时看 |
| `specs/001-frontstage-mvp/tasks.md` | 具体开发任务 checklist | 写代码或查任务时看 |
| `specs/001-frontstage-mvp/plan.md` | 技术方案和实现计划 | 需要理解技术设计时看 |
| `docs/01-需求文档-东南亚新品流程.md` | 业务需求主文档 | 需要查业务背景时看 |
| `docs/21-后半段需求领导对齐问题清单.md` | 现行后半段已确认口径与仍待业务回答的问题 | 后段对齐时先看 |
| `docs/18-市场监控表字段来源确认问题清单.md` | 市场监控字段来源矩阵与字段级开放问题 | 对齐字段来源时看 |

简单说：

- `20` 管推进。
- `AGENT_HANDOFF` 管新会话交接。
- `02` 管完成状态。
- `tasks` 管开发 checklist。
- `plan` 管技术设计。

## 6. 新会话推荐开场

```text
请先读取：
AGENT_HANDOFF.md
docs/20-项目推进总控.md
docs/02-功能实现状态.md

第一版只导出、不做在线表自动写回已经确认，不要再问。
请先说明当前完成度和下一步，然后从开发环境业务验收反馈开始推进。
```
